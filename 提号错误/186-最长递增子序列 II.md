# 最长递增子序列 II

## 题目描述
给定一个整数数组 nums 和一个整数 k，找到最长递增子序列，要求子序列中相邻元素之间的差值不超过 k。

示例 1:
```
输入: nums = [4,2,1,4,3,4,5,8,15], k = 3
输出: 5
解释: 最长递增子序列是 [1,3,4,5,8]，相邻元素之间的差值都不超过3。
```

示例 2:
```
输入: nums = [7,4,5,1,8,12,4,7], k = 5
输出: 4
解释: 最长递增子序列是 [4,5,8,12]，相邻元素之间的差值都不超过5。
```

## 解题思路
1. 动态规划：
   - dp[i]表示以nums[i]结尾的最长递增子序列的长度
   - 状态转移方程：dp[i] = max(dp[j]) + 1，其中0 ≤ j < i且nums[j] < nums[i]且nums[i] - nums[j] ≤ k
   - 时间复杂度：O(n²)，空间复杂度：O(n)

2. 线段树优化：
   - 使用线段树维护区间最大值
   - 对于每个数，查询区间[nums[i]-k, nums[i]-1]的最大值
   - 时间复杂度：O(n log n)，空间复杂度：O(n)

## Java实现
```java
class Solution {
    // 动态规划
    public int lengthOfLIS(int[] nums, int k) {
        if (nums == null || nums.length == 0) return 0;
        int n = nums.length;
        int[] dp = new int[n];
        Arrays.fill(dp, 1);
        int maxLen = 1;
        
        for (int i = 1; i < n; i++) {
            for (int j = 0; j < i; j++) {
                if (nums[j] < nums[i] && nums[i] - nums[j] <= k) {
                    dp[i] = Math.max(dp[i], dp[j] + 1);
                }
            }
            maxLen = Math.max(maxLen, dp[i]);
        }
        
        return maxLen;
    }
    
    // 线段树优化
    public int lengthOfLISWithSegmentTree(int[] nums, int k) {
        if (nums == null || nums.length == 0) return 0;
        int n = nums.length;
        int maxNum = Arrays.stream(nums).max().getAsInt();
        SegmentTree st = new SegmentTree(maxNum + 1);
        int[] dp = new int[n];
        int maxLen = 1;
        
        for (int i = 0; i < n; i++) {
            int left = Math.max(0, nums[i] - k);
            int right = nums[i] - 1;
            dp[i] = st.query(left, right) + 1;
            st.update(nums[i], dp[i]);
            maxLen = Math.max(maxLen, dp[i]);
        }
        
        return maxLen;
    }
}

class SegmentTree {
    private int[] tree;
    private int n;
    
    public SegmentTree(int n) {
        this.n = n;
        this.tree = new int[4 * n];
    }
    
    public void update(int index, int value) {
        update(0, 0, n - 1, index, value);
    }
    
    private void update(int node, int start, int end, int index, int value) {
        if (start == end) {
            tree[node] = Math.max(tree[node], value);
            return;
        }
        
        int mid = (start + end) / 2;
        if (index <= mid) {
            update(2 * node + 1, start, mid, index, value);
        } else {
            update(2 * node + 2, mid + 1, end, index, value);
        }
        
        tree[node] = Math.max(tree[2 * node + 1], tree[2 * node + 2]);
    }
    
    public int query(int left, int right) {
        return query(0, 0, n - 1, left, right);
    }
    
    private int query(int node, int start, int end, int left, int right) {
        if (start > right || end < left) {
            return 0;
        }
        
        if (start >= left && end <= right) {
            return tree[node];
        }
        
        int mid = (start + end) / 2;
        return Math.max(
            query(2 * node + 1, start, mid, left, right),
            query(2 * node + 2, mid + 1, end, left, right)
        );
    }
}
```

## Python实现
```python
class Solution:
    def lengthOfLIS(self, nums: List[int], k: int) -> int:
        if not nums:
            return 0
            
        n = len(nums)
        dp = [1] * n
        max_len = 1
        
        for i in range(1, n):
            for j in range(i):
                if nums[j] < nums[i] and nums[i] - nums[j] <= k:
                    dp[i] = max(dp[i], dp[j] + 1)
            max_len = max(max_len, dp[i])
            
        return max_len
        
    def lengthOfLISWithSegmentTree(self, nums: List[int], k: int) -> int:
        if not nums:
            return 0
            
        n = len(nums)
        max_num = max(nums)
        st = SegmentTree(max_num + 1)
        dp = [1] * n
        max_len = 1
        
        for i in range(n):
            left = max(0, nums[i] - k)
            right = nums[i] - 1
            dp[i] = st.query(left, right) + 1
            st.update(nums[i], dp[i])
            max_len = max(max_len, dp[i])
            
        return max_len

class SegmentTree:
    def __init__(self, n):
        self.n = n
        self.tree = [0] * (4 * n)
        
    def update(self, index, value):
        self._update(0, 0, self.n - 1, index, value)
        
    def _update(self, node, start, end, index, value):
        if start == end:
            self.tree[node] = max(self.tree[node], value)
            return
            
        mid = (start + end) // 2
        if index <= mid:
            self._update(2 * node + 1, start, mid, index, value)
        else:
            self._update(2 * node + 2, mid + 1, end, index, value)
            
        self.tree[node] = max(self.tree[2 * node + 1], self.tree[2 * node + 2])
        
    def query(self, left, right):
        return self._query(0, 0, self.n - 1, left, right)
        
    def _query(self, node, start, end, left, right):
        if start > right or end < left:
            return 0
            
        if start >= left and end <= right:
            return self.tree[node]
            
        mid = (start + end) // 2
        return max(
            self._query(2 * node + 1, start, mid, left, right),
            self._query(2 * node + 2, mid + 1, end, left, right)
        )
```

## 复杂度分析
- 动态规划：
  - 时间复杂度：O(n²)
  - 空间复杂度：O(n)
- 线段树优化：
  - 时间复杂度：O(n log n)
  - 空间复杂度：O(n)

## 相关题目总结
1. 子序列相关题目：
   - 最长递增子序列
   - 最长递增子序列的个数
   - 最长递增子序列 II

2. 解题技巧：
   - 动态规划
   - 线段树
   - 区间查询
   - 处理差值限制
   - 优化时间复杂度 