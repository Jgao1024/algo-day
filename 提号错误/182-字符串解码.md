# 字符串解码

## 题目描述
给定一个经过编码的字符串，返回它解码后的字符串。

编码规则为: k[encoded_string]，表示其中方括号内部的 encoded_string 正好重复 k 次。注意 k 保证为正整数。

你可以认为输入字符串总是有效的；输入字符串中没有额外的空格，且输入的方括号总是符合格式要求的。

此外，你可以认为原始数据不包含数字，所有的数字只表示重复的次数 k ，例如不会出现像 3a 或 2[4] 的输入。

示例 1:
```
输入：s = "3[a]2[bc]"
输出："aaabcbc"
```

示例 2:
```
输入：s = "3[a2[c]]"
输出："accaccacc"
```

## 解题思路
1. 使用栈：
   - 遇到数字，解析出完整的数字
   - 遇到字母，直接加入结果
   - 遇到'['，将当前数字和结果入栈
   - 遇到']'，弹出栈顶的数字和字符串，进行重复
   - 时间复杂度：O(n)，空间复杂度：O(n)

2. 关键点：
   - 处理嵌套的编码
   - 处理多位数字
   - 处理字符串拼接

## Java实现
```java
class Solution {
    public String decodeString(String s) {
        Stack<Integer> numStack = new Stack<>();
        Stack<String> strStack = new Stack<>();
        StringBuilder result = new StringBuilder();
        int num = 0;
        
        for (char c : s.toCharArray()) {
            if (Character.isDigit(c)) {
                num = num * 10 + (c - '0');
            } else if (c == '[') {
                numStack.push(num);
                strStack.push(result.toString());
                num = 0;
                result = new StringBuilder();
            } else if (c == ']') {
                int repeat = numStack.pop();
                String prev = strStack.pop();
                StringBuilder temp = new StringBuilder();
                for (int i = 0; i < repeat; i++) {
                    temp.append(result);
                }
                result = new StringBuilder(prev + temp.toString());
            } else {
                result.append(c);
            }
        }
        
        return result.toString();
    }
}
```

## Python实现
```python
class Solution:
    def decodeString(self, s: str) -> str:
        num_stack = []
        str_stack = []
        result = []
        num = 0
        
        for c in s:
            if c.isdigit():
                num = num * 10 + int(c)
            elif c == '[':
                num_stack.append(num)
                str_stack.append(''.join(result))
                num = 0
                result = []
            elif c == ']':
                repeat = num_stack.pop()
                prev = str_stack.pop()
                result = [prev] + result * repeat
            else:
                result.append(c)
                
        return ''.join(result)
```

## 复杂度分析
- 时间复杂度：O(n)，需要遍历整个字符串
- 空间复杂度：O(n)，需要使用栈存储中间结果

## 相关题目总结
1. 字符串处理类题目：
   - 字符串解码
   - 括号匹配
   - 字符串压缩

2. 解题技巧：
   - 使用栈处理嵌套结构
   - 处理数字和字符串的转换
   - 处理字符串拼接
   - 考虑边界情况 