# 最长回文子序列

## 题目描述
给定一个字符串s，找到其中最长的回文子序列的长度。子序列是指从原字符串中删除一些字符（可以不删除）而不改变其余字符的相对位置形成的序列。

示例：
```
输入：s = "bbbab"
输出：4
解释：最长的回文子序列是 "bbbb"。

输入：s = "cbbd"
输出：2
解释：最长的回文子序列是 "bb"。
```

## 解题思路
1. 动态规划法：
   - 定义dp[i][j]表示s[i...j]的最长回文子序列长度
   - 状态转移方程：
     - 如果s[i] == s[j]，dp[i][j] = dp[i+1][j-1] + 2
     - 否则，dp[i][j] = max(dp[i+1][j], dp[i][j-1])
   - 时间复杂度：O(n²)，空间复杂度：O(n²)

2. 空间优化方法：
   - 使用滚动数组优化空间
   - 时间复杂度：O(n²)，空间复杂度：O(n)

## Java实现
```java
class Solution {
    // 动态规划法
    public int longestPalindromeSubseq(String s) {
        int n = s.length();
        int[][] dp = new int[n][n];
        
        // 初始化对角线
        for (int i = 0; i < n; i++) {
            dp[i][i] = 1;
        }
        
        // 填充dp数组
        for (int len = 2; len <= n; len++) {
            for (int i = 0; i <= n - len; i++) {
                int j = i + len - 1;
                if (s.charAt(i) == s.charAt(j)) {
                    dp[i][j] = dp[i + 1][j - 1] + 2;
                } else {
                    dp[i][j] = Math.max(dp[i + 1][j], dp[i][j - 1]);
                }
            }
        }
        
        return dp[0][n - 1];
    }
    
    // 空间优化方法
    public int longestPalindromeSubseqOptimized(String s) {
        int n = s.length();
        int[] dp = new int[n];
        
        // 初始化
        for (int i = 0; i < n; i++) {
            dp[i] = 1;
        }
        
        // 填充dp数组
        for (int i = n - 1; i >= 0; i--) {
            int prev = 0;
            for (int j = i + 1; j < n; j++) {
                int temp = dp[j];
                if (s.charAt(i) == s.charAt(j)) {
                    dp[j] = prev + 2;
                } else {
                    dp[j] = Math.max(dp[j], dp[j - 1]);
                }
                prev = temp;
            }
        }
        
        return dp[n - 1];
    }
}
```

## Python实现
```python
class Solution:
    # 动态规划法
    def longestPalindromeSubseq(self, s: str) -> int:
        n = len(s)
        dp = [[0] * n for _ in range(n)]
        
        # 初始化对角线
        for i in range(n):
            dp[i][i] = 1
        
        # 填充dp数组
        for length in range(2, n + 1):
            for i in range(n - length + 1):
                j = i + length - 1
                if s[i] == s[j]:
                    dp[i][j] = dp[i + 1][j - 1] + 2
                else:
                    dp[i][j] = max(dp[i + 1][j], dp[i][j - 1])
        
        return dp[0][n - 1]
    
    # 空间优化方法
    def longestPalindromeSubseqOptimized(self, s: str) -> int:
        n = len(s)
        dp = [1] * n
        
        # 填充dp数组
        for i in range(n - 1, -1, -1):
            prev = 0
            for j in range(i + 1, n):
                temp = dp[j]
                if s[i] == s[j]:
                    dp[j] = prev + 2
                else:
                    dp[j] = max(dp[j], dp[j - 1])
                prev = temp
        
        return dp[n - 1]
```

## 复杂度分析
- 动态规划法：
  - 时间复杂度：O(n²)，其中n是字符串长度
  - 空间复杂度：O(n²)
- 空间优化方法：
  - 时间复杂度：O(n²)
  - 空间复杂度：O(n)

## 相关题目总结
1. 回文序列相关题目：
   - 最长回文子序列
   - 最长回文子串
   - 回文数
   - 验证回文串

2. 解题技巧：
   - 动态规划
   - 空间优化
   - 字符串处理
   - 子序列问题 