# 931. 下降路径最小和

- 题号：931
- 难度：中等
- LeetCode：[下降路径最小和](https://leetcode.cn/problems/minimum-falling-path-sum/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：DP  
`dp[i][j]` 表示到达 `(i,j)` 的最小和：来自上一行的 `j-1/j/j+1` 取最小。用一维数组滚动即可。

## Java 最佳实现

```java
import java.util.Arrays;

class Solution {
    public int minFallingPathSum(int[][] matrix) {
        int n = matrix.length;
        int[] dp = Arrays.copyOf(matrix[0], n);

        for (int i = 1; i < n; i++) {
            int[] ndp = new int[n];
            for (int j = 0; j < n; j++) {
                int best = dp[j];
                if (j > 0) {
                    best = Math.min(best, dp[j - 1]);
                }
                if (j + 1 < n) {
                    best = Math.min(best, dp[j + 1]);
                }
                ndp[j] = best + matrix[i][j];
            }
            dp = ndp;
        }

        int ans = dp[0];
        for (int v : dp) {
            ans = Math.min(ans, v);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n^2)\)
- 空间：\(O(n)\)

## 相关题目
- 120 三角形最小路径和：类似 DP
