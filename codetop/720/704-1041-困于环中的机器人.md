# 1041. 困于环中的机器人

- 题号：1041
- 难度：中等
- LeetCode：[困于环中的机器人](https://leetcode.cn/problems/robot-bounded-in-circle/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 模拟走一遍指令：如果回到原点，或最终方向不是“北”，则会在圈内循环；否则不会。

## Java 最佳实现

```java
class Solution {
    public boolean isRobotBounded(String instructions) {
        int x = 0, y = 0;
        int dir = 0; // 0:N 1:E 2:S 3:W
        int[][] d = {{0, 1}, {1, 0}, {0, -1}, {-1, 0}};
        for (int i = 0; i < instructions.length(); i++) {
            char c = instructions.charAt(i);
            if (c == 'G') {
                x += d[dir][0];
                y += d[dir][1];
            } else if (c == 'L') {
                dir = (dir + 3) & 3;
            } else {
                dir = (dir + 1) & 3;
            }
        }
        return (x == 0 && y == 0) || dir != 0;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 874 模拟行走机器人：同类模拟题
