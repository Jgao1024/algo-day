# 575. 分糖果

- 题号：575
- 难度：简单
- LeetCode：[分糖果](https://leetcode.cn/problems/distribute-candies/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 女孩最多拿到 `n/2` 颗糖；尽量多拿不同种类，所以答案是 `min(种类数, n/2)`。

## Java 最佳实现

```java
import java.util.HashSet;
import java.util.Set;

class Solution {
    public int distributeCandies(int[] candyType) {
        Set<Integer> set = new HashSet<>();
        for (int x : candyType) set.add(x);
        return Math.min(set.size(), candyType.length / 2);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 349 两个数组的交集：集合去重
