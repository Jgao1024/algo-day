# 173. 二叉搜索树迭代器

- 题号：173
- 难度：中等
- LeetCode：[二叉搜索树迭代器](https://leetcode.cn/problems/binary-search-tree-iterator/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：栈模拟中序遍历  
初始化把根节点一路压左链。`next()` 弹出栈顶（当前最小），再把它的右子树一路压左链即可。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Deque;

class BSTIterator {
    private final Deque<TreeNode> stack = new ArrayDeque<>();

    public BSTIterator(TreeNode root) {
        pushLeft(root);
    }

    public int next() {
        TreeNode node = stack.pop();
        pushLeft(node.right);
        return node.val;
    }

    public boolean hasNext() {
        return !stack.isEmpty();
    }

    private void pushLeft(TreeNode node) {
        while (node != null) {
            stack.push(node);
            node = node.left;
        }
    }
}
```

## 复杂度分析
- 时间：均摊 \(O(1)\)（每个节点最多入栈出栈一次）
- 空间：\(O(h)\)

## 相关题目
- 94 二叉树的中序遍历
