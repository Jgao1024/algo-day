# 321. 拼接最大数

- 题号：321
- 难度：困难
- LeetCode：[拼接最大数](https://leetcode.cn/problems/create-maximum-number/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：枚举分配 + 单调栈取子序列 + 合并  
枚举从 `nums1` 取 `i` 个、从 `nums2` 取 `k-i` 个（合法范围内）。  
对每个数组用单调栈取长度为指定值的最大子序列；再把两个子序列按字典序规则合并成最大序列；取全局最大即可。

## Java 最佳实现

```java
class Solution {
    public int[] maxNumber(int[] nums1, int[] nums2, int k) {
        int n1 = nums1.length;
        int n2 = nums2.length;
        int[] best = new int[k];

        int start = Math.max(0, k - n2);
        int end = Math.min(k, n1);
        for (int i = start; i <= end; i++) {
            int[] a = pickMax(nums1, i);
            int[] b = pickMax(nums2, k - i);
            int[] merged = merge(a, b);
            if (greater(merged, 0, best, 0)) {
                best = merged;
            }
        }
        return best;
    }

    private int[] pickMax(int[] nums, int k) {
        int n = nums.length;
        int drop = n - k;
        int[] stack = new int[n];
        int top = 0;
        for (int x : nums) {
            while (top > 0 && drop > 0 && stack[top - 1] < x) {
                top--;
                drop--;
            }
            stack[top++] = x;
        }
        int[] res = new int[k];
        System.arraycopy(stack, 0, res, 0, k);
        return res;
    }

    private int[] merge(int[] a, int[] b) {
        int[] res = new int[a.length + b.length];
        int i = 0, j = 0, t = 0;
        while (i < a.length || j < b.length) {
            if (greater(a, i, b, j)) {
                res[t++] = a[i++];
            } else {
                res[t++] = b[j++];
            }
        }
        return res;
    }

    private boolean greater(int[] a, int i, int[] b, int j) {
        while (i < a.length && j < b.length && a[i] == b[j]) {
            i++;
            j++;
        }
        if (j == b.length) {
            return true;
        }
        if (i == a.length) {
            return false;
        }
        return a[i] > b[j];
    }
}
```

## 复杂度分析
- 时间：约 \(O((m+n)k)\)
- 空间：\(O(k)\)

## 相关题目
- 402 移掉 K 位数字：单调栈
