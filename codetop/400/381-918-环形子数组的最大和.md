# 918. 环形子数组的最大和

- 题号：918
- 难度：中等
- LeetCode：[环形子数组的最大和](https://leetcode.cn/problems/maximum-sum-circular-subarray/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：Kadane + 最小子数组  
答案要么是“普通最大子数组和”，要么是“跨环的最大和”。  
跨环最大和 = `total - minSubarray`（等价于取一段最小子数组丢掉）。  
但如果全是负数，`total - minSubarray` 会变成 0，需要单独返回普通最大子数组和。

## Java 最佳实现

```java
class Solution {
    public int maxSubarraySumCircular(int[] nums) {
        int total = 0;
        int curMax = 0, maxSum = nums[0];
        int curMin = 0, minSum = nums[0];

        for (int x : nums) {
            total += x;

            curMax = Math.max(x, curMax + x);
            maxSum = Math.max(maxSum, curMax);

            curMin = Math.min(x, curMin + x);
            minSum = Math.min(minSum, curMin);
        }

        if (maxSum < 0) {
            return maxSum;
        }
        return Math.max(maxSum, total - minSum);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 53 最大子数组和
