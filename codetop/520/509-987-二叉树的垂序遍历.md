# 987. 二叉树的垂序遍历

- 题号：987
- 难度：中等
- LeetCode：[二叉树的垂序遍历](https://leetcode.cn/problems/vertical-order-traversal-of-a-binary-tree/)

## 题目描述
给定二叉树 `root`，按“垂序遍历”返回节点值：  
每个节点有坐标 `(row, col)`，根为 `(0,0)`，左子为 `(row+1, col-1)`，右子为 `(row+1, col+1)`。  
要求：按 `col` 从小到大输出；同一列内按 `row` 从小到大；若 `row/col` 都相同，则按值从小到大。

## 示例
- 示例  
输入：`root = [3,9,20,null,null,15,7]`  
输出：`[[9],[3,15],[20],[7]]`

## 最佳解法思路
- 思路1（推荐）：DFS 收集坐标后排序  
遍历树，收集三元组 `(col, row, val)`。  
对列表排序：先 `col`，再 `row`，最后 `val`，然后按 `col` 分组输出。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

class Solution {
    public List<List<Integer>> verticalTraversal(TreeNode root) {
        List<int[]> list = new ArrayList<>();
        dfs(root, 0, 0, list);
        Collections.sort(list, (a, b) -> {
            if (a[0] != b[0]) return a[0] - b[0];
            if (a[1] != b[1]) return a[1] - b[1];
            return a[2] - b[2];
        });

        List<List<Integer>> res = new ArrayList<>();
        Integer lastCol = null;
        for (int[] t : list) {
            if (lastCol == null || t[0] != lastCol) {
                res.add(new ArrayList<>());
                lastCol = t[0];
            }
            res.get(res.size() - 1).add(t[2]);
        }
        return res;
    }

    private void dfs(TreeNode node, int row, int col, List<int[]> list) {
        if (node == null) return;
        list.add(new int[]{col, row, node.val});
        dfs(node.left, row + 1, col - 1, list);
        dfs(node.right, row + 1, col + 1, list);
    }
}
```

## 复杂度分析
- 时间：\(O(n\log n)\)（排序）
- 空间：\(O(n)\)

## 相关题目
- 314 二叉树的垂直遍历：同列分组（排序规则更宽松）
- 102 二叉树的层序遍历：遍历基础

## 总结（速记）
- 先收集坐标再统一排序：`col -> row -> val`，最后按 `col` 分组。 