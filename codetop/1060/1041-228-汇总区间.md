# 228. 汇总区间

- 题号：228
- 难度：中等
- LeetCode：[汇总区间](https://leetcode.cn/problems/summary-ranges/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **双指针扫描连续段**  
从左到右找到每一段连续递增（差为 1）的区间 `[start..end]`：  
  - 若 `start==end` 输出单点  
  - 否则输出 `start->end`

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<String> summaryRanges(int[] nums) {
        List<String> ans = new ArrayList<>();
        int i = 0;
        while (i < nums.length) {
            int start = nums[i];
            int j = i;
            while (j + 1 < nums.length && nums[j + 1] == nums[j] + 1) {
                j++;
            }
            int end = nums[j];
            if (start == end) ans.add(String.valueOf(start));
            else ans.add(start + "->" + end);
            i = j + 1;
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)（不计返回）

## 相关题目
- 56 合并区间：区间合并/扫描
