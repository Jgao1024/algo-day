# 1552. 两球之间的磁力

- 题号：1552
- 难度：中等
- LeetCode：[两球之间的磁力](https://leetcode.cn/problems/magnetic-force-between-two-balls/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **二分答案 + 贪心放球**  
最小磁力越大，能放下的球越少（单调性）。对答案 `d` 二分：  
把位置排序后，从左到右贪心放球，能放下至少 `m` 个则可行。

## Java 最佳实现

```java
import java.util.Arrays;

class Solution {
    public int maxDistance(int[] position, int m) {
        Arrays.sort(position);
        int l = 1;
        int r = position[position.length - 1] - position[0];
        while (l < r) {
            int mid = l + (r - l + 1) / 2;
            if (can(position, m, mid)) l = mid;
            else r = mid - 1;
        }
        return l;
    }

    private boolean can(int[] pos, int m, int d) {
        int count = 1;
        int last = pos[0];
        for (int i = 1; i < pos.length; i++) {
            if (pos[i] - last >= d) {
                count++;
                last = pos[i];
                if (count >= m) return true;
            }
        }
        return count >= m;
    }
}
```

## 复杂度分析
- 时间：\(O(n \log D)\)
- 空间：\(O(1)\)

## 相关题目
- 1482 制作 m 束花所需的最少天数：二分答案 + 校验
