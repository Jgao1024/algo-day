# 303. 区域和检索 - 数组不可变

- 题号：303
- 难度：简单
- LeetCode：[区域和检索 - 数组不可变](https://leetcode.cn/problems/range-sum-query-immutable/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **前缀和**  
预处理 `pre[i]` 表示 `nums[0..i-1]` 的和，则区间 `[l,r]` 的和为 `pre[r+1] - pre[l]`。

## Java 最佳实现

```java
class NumArray {
    private final int[] pre;

    public NumArray(int[] nums) {
        pre = new int[nums.length + 1];
        for (int i = 0; i < nums.length; i++) {
            pre[i + 1] = pre[i] + nums[i];
        }
    }

    public int sumRange(int left, int right) {
        return pre[right + 1] - pre[left];
    }
}
```

## 复杂度分析
- 时间：构造 \(O(n)\)，查询 \(O(1)\)
- 空间：\(O(n)\)

## 相关题目
- 304 二维区域和检索：二维前缀和
- 560 和为 K 的子数组：前缀和 + 哈希表
