# 513. 找树左下角的值

- 题号：513
- 难度：中等
- LeetCode：[找树左下角的值](https://leetcode.cn/problems/find-bottom-left-tree-value/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **BFS（每层第一个）**  
层序遍历每一层，记录该层第一个节点的值，最后一层的第一个值就是答案。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Queue;

class Solution {
    public int findBottomLeftValue(TreeNode root) {
        Queue<TreeNode> q = new ArrayDeque<>();
        q.add(root);
        int ans = root.val;
        while (!q.isEmpty()) {
            int size = q.size();
            for (int i = 0; i < size; i++) {
                TreeNode cur = q.poll();
                if (i == 0) ans = cur.val;
                if (cur.left != null) q.add(cur.left);
                if (cur.right != null) q.add(cur.right);
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 102 二叉树的层序遍历
- 199 二叉树的右视图
