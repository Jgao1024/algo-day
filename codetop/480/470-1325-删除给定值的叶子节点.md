# 1325. 删除给定值的叶子节点

- 题号：1325
- 难度：中等
- LeetCode：[删除给定值的叶子节点](https://leetcode.cn/problems/delete-leaves-with-a-given-value/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **后序递归**  
先递归处理左右子树，拿到“删除后的子树”。  
如果当前节点左右都为空且值等于 `target`，说明它变成了要删的叶子，返回 `null`；否则返回当前节点。

## Java 最佳实现

```java
class Solution {
    public TreeNode removeLeafNodes(TreeNode root, int target) {
        if (root == null) return null;
        root.left = removeLeafNodes(root.left, target);
        root.right = removeLeafNodes(root.right, target);
        if (root.left == null && root.right == null && root.val == target) return null;
        return root;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)

## 相关题目
- 814 二叉树剪枝：同类后序删除节点
