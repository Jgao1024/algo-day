# 88. 合并两个有序数组

- 题号：88
- 难度：简单
- LeetCode：[合并两个有序数组](https://leetcode.cn/problems/merge-sorted-array/)

## 题目描述
给你两个按非递减顺序排列的整数数组 `nums1` 和 `nums2`。

`nums1` 的长度为 `m + n`，其中前 `m` 个元素表示有效元素，后 `n` 个元素为 0 占位；`nums2` 长度为 `n`。

请你把 `nums2` 合并到 `nums1` 中，使 `nums1` 成为一个有序数组（原地修改）。

## 示例
- 示例1  
输入：`nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3`  
输出：`[1,2,2,3,5,6]`

- 示例2  
输入：`nums1 = [1], m = 1, nums2 = [], n = 0`  
输出：`[1]`

- 示例3  
输入：`nums1 = [0], m = 0, nums2 = [1], n = 1`  
输出：`[1]`

## 最佳解法思路
- 思路1（推荐）：从后往前双指针  
从 `nums1[m-1]` 和 `nums2[n-1]` 开始比较，把较大的放到 `nums1[m+n-1]`，指针前移。这样不会覆盖 `nums1` 的有效数据。

- 思路2：先拷贝 nums1 再从前往后合并（不推荐）  
需要额外数组空间。

## Java 最佳实现

```java
class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        int i = m - 1;
        int j = n - 1;
        int k = m + n - 1;

        while (j >= 0) {
            if (i >= 0 && nums1[i] > nums2[j]) {
                nums1[k--] = nums1[i--];
            } else {
                nums1[k--] = nums2[j--];
            }
        }
    }
}
```

## 复杂度分析
- 时间：\(O(m+n)\)
- 空间：\(O(1)\)

## 相关题目
- 21 合并两个有序链表：链表版合并
- 88/21 对比：数组从后往前更省事，链表从前往后直接接
- 977 有序数组的平方：双指针从两端写入

## 总结（速记）
- “nums1 有空位”的合并题，第一反应就是**从后往前写**。
