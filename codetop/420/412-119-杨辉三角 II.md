# 119. 杨辉三角 II

- 题号：119
- 难度：简单
- LeetCode：[杨辉三角 II](https://leetcode.cn/problems/pascals-triangle-ii/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 一维 DP：第 `i` 行只依赖上一行，从后往前更新：`row[j] = row[j] + row[j-1]`。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<Integer> getRow(int rowIndex) {
        int[] row = new int[rowIndex + 1];
        row[0] = 1;
        for (int i = 1; i <= rowIndex; i++) {
            row[i] = 1;
            for (int j = i - 1; j >= 1; j--) {
                row[j] = row[j] + row[j - 1];
            }
        }

        List<Integer> res = new ArrayList<>(rowIndex + 1);
        for (int v : row) res.add(v);
        return res;
    }
}
```

## 复杂度分析
- 时间：\(O(k^2)\)
- 空间：\(O(k)\)

## 相关题目
- 118 杨辉三角：输出前 n 行
