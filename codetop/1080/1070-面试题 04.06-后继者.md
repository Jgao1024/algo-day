# 面试题 04.06. 后继者

- 题号：面试题 04.06
- 难度：中等
- LeetCode：[后继者](https://leetcode.cn/problems/successor-lcci/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：BST 中序后继  
若 `p` 有右子树，后继是右子树最左节点。  
否则从根开始：当 `root.val > p.val`，记录当前为候选后继并向左走；否则向右走。

## Java 最佳实现

```java
class Solution {
    public TreeNode inorderSuccessor(TreeNode root, TreeNode p) {
        if (p == null) {
            return null;
        }
        if (p.right != null) {
            TreeNode cur = p.right;
            while (cur.left != null) {
                cur = cur.left;
            }
            return cur;
        }

        TreeNode succ = null;
        TreeNode cur = root;
        while (cur != null) {
            if (cur.val > p.val) {
                succ = cur;
                cur = cur.left;
            } else {
                cur = cur.right;
            }
        }
        return succ;
    }
}
```

## 复杂度分析
- 时间：\(O(h)\)
- 空间：\(O(1)\)

## 相关题目
- 285 二叉搜索树中的中序后继
