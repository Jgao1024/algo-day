# 1166. 设计文件系统

- 题号：1166
- 难度：中等
- LeetCode：[设计文件系统](https://leetcode.cn/problems/design-file-system/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：HashMap 存路径值  
`createPath(path, value)`：  
- 若已存在返回 false  
- 取父路径 `parent`，父不存在返回 false（`/` 作为根默认存在）  
否则写入并返回 true。  
`get(path)`：不存在返回 -1。

## Java 最佳实现

```java
import java.util.HashMap;
import java.util.Map;

class FileSystem {
    private final Map<String, Integer> map = new HashMap<>();

    public FileSystem() {
        map.put("/", 0);
    }

    public boolean createPath(String path, int value) {
        if (path == null || path.length() == 0 || "/".equals(path)) {
            return false;
        }
        if (map.containsKey(path)) {
            return false;
        }

        int idx = path.lastIndexOf('/');
        String parent = (idx == 0) ? "/" : path.substring(0, idx);
        if (!map.containsKey(parent)) {
            return false;
        }

        map.put(path, value);
        return true;
    }

    public int get(String path) {
        Integer v = map.get(path);
        return v == null ? -1 : v;
    }
}
```

## 复杂度分析
- 时间：每次操作 \(O(L)\)，\(L\) 为路径长度
- 空间：\(O(\text{路径数})\)

## 相关题目
- 588 设计内存文件系统
