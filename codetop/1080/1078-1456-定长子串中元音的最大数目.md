# 1456. 定长子串中元音的最大数目

- 题号：1456
- 难度：中等
- LeetCode：[定长子串中元音的最大数目](https://leetcode.cn/problems/maximum-number-of-vowels-in-a-substring-of-given-length/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：固定窗口  
维护长度为 `k` 的窗口元音数量，滑动时加新减旧，取最大值。

## Java 最佳实现

```java
class Solution {
    public int maxVowels(String s, int k) {
        int ans = 0;
        int cur = 0;
        for (int i = 0; i < s.length(); i++) {
            cur += isVowel(s.charAt(i)) ? 1 : 0;
            if (i >= k) {
                cur -= isVowel(s.charAt(i - k)) ? 1 : 0;
            }
            if (i >= k - 1) {
                ans = Math.max(ans, cur);
            }
        }
        return ans;
    }

    private boolean isVowel(char c) {
        return c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u';
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 1100 长度为 K 的无重复字符子串：固定窗口
