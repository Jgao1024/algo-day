# 648. 单词替换

- 题号：648
- 难度：中等
- LeetCode：[单词替换](https://leetcode.cn/problems/replace-words/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：Trie  
把所有词根建成 Trie。对句子中每个单词从前往后在 Trie 里走：  
若走到某个词根结束位置，就用该词根替换；否则保留原词。

## Java 最佳实现

```java
import java.util.List;

class Solution {
    public String replaceWords(List<String> dictionary, String sentence) {
        Trie trie = new Trie();
        for (String w : dictionary) {
            trie.insert(w);
        }

        String[] words = sentence.split(" ");
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < words.length; i++) {
            if (i > 0) {
                sb.append(' ');
            }
            sb.append(trie.replace(words[i]));
        }
        return sb.toString();
    }

    private static class Trie {
        Node root = new Node();

        void insert(String s) {
            Node cur = root;
            for (int i = 0; i < s.length(); i++) {
                int idx = s.charAt(i) - 'a';
                if (cur.next[idx] == null) {
                    cur.next[idx] = new Node();
                }
                cur = cur.next[idx];
            }
            cur.end = true;
        }

        String replace(String word) {
            Node cur = root;
            StringBuilder sb = new StringBuilder();
            for (int i = 0; i < word.length(); i++) {
                int idx = word.charAt(i) - 'a';
                if (cur.next[idx] == null) {
                    return word;
                }
                sb.append(word.charAt(i));
                cur = cur.next[idx];
                if (cur.end) {
                    return sb.toString();
                }
            }
            return word;
        }
    }

    private static class Node {
        Node[] next = new Node[26];
        boolean end;
    }
}
```

## 复杂度分析
- 时间：\(O(\text{总字符数})\)
- 空间：\(O(\text{Trie 大小})\)

## 相关题目
- 208 实现 Trie
