# 569. 员工薪水中位数

- 题号：569
- 难度：困难
- LeetCode：[员工薪水中位数](https://leetcode.cn/problems/median-employee-salary/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 窗口函数排序取“中位数位置”  
对每个 `Company` 内按 `Salary, Id` 排序，得到 `row_number` 和总人数 `cnt`。  
中位数对应的位置是：\((cnt+1)/2\) 和 \((cnt+2)/2\)（奇数时两者相同，偶数时取中间两行）。

## SQL 最佳实现

```sql
WITH t AS (
    SELECT Id,
           Company,
           Salary,
           ROW_NUMBER() OVER (PARTITION BY Company ORDER BY Salary, Id) AS rn,
           COUNT(*) OVER (PARTITION BY Company) AS cnt
    FROM Employee
)
SELECT Id, Company, Salary
FROM t
WHERE rn IN ((cnt + 1) / 2, (cnt + 2) / 2)
ORDER BY Company, Salary, Id;
```

## 复杂度分析
- 时间：取决于执行计划（通常是按公司分组排序）
- 空间：取决于执行计划

## 相关题目
- 185 部门工资前三高的所有员工：窗口函数
- 1204 最后一个能进入电梯的人：窗口函数
