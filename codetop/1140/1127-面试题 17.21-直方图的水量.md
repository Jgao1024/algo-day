# 面试题 17.21. 直方图的水量

- 题号：面试题 17.21
- 难度：困难
- LeetCode：[直方图的水量](https://leetcode.cn/problems/volume-of-histogram-lcci/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 双指针 + 维护左右最高柱  
`l/r` 从两端向中间：  
- 若 `height[l] <= height[r]`，水量由 `leftMax` 决定：`max(0, leftMax - height[l])`，然后更新 `leftMax`、`l++`  
- 否则对称处理右侧  
一次遍历即可。

## Java 最佳实现

```java
class Solution {
    public int trap(int[] height) {
        int l = 0, r = height.length - 1;
        int leftMax = 0, rightMax = 0;
        int ans = 0;
        while (l < r) {
            if (height[l] <= height[r]) {
                leftMax = Math.max(leftMax, height[l]);
                ans += leftMax - height[l];
                l++;
            } else {
                rightMax = Math.max(rightMax, height[r]);
                ans += rightMax - height[r];
                r--;
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 42 接雨水：同题
- 84 柱状图中最大的矩形：单调栈
