# 919. 完全二叉树插入器

- 题号：919
- 难度：中等
- LeetCode：[完全二叉树插入器](https://leetcode.cn/problems/complete-binary-tree-inserter/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 队列维护“第一个不满的节点”  
初始化时做一次层序遍历，把所有缺孩子的节点按顺序入队。  
插入时始终操作队首：优先补左孩子；左满再补右孩子，补完右孩子后该节点出队。新插入的节点也会变成“不满节点”，入队即可。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Deque;

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class CBTInserter {
    private final TreeNode root;
    private final Deque<TreeNode> q = new ArrayDeque<>();

    public CBTInserter(TreeNode root) {
        this.root = root;
        Deque<TreeNode> bfs = new ArrayDeque<>();
        bfs.offer(root);
        while (!bfs.isEmpty()) {
            TreeNode node = bfs.poll();
            if (node.left != null) bfs.offer(node.left);
            if (node.right != null) bfs.offer(node.right);
            if (node.left == null || node.right == null) q.offer(node);
        }
    }

    public int insert(int val) {
        TreeNode parent = q.peek();
        TreeNode node = new TreeNode(val);
        if (parent.left == null) {
            parent.left = node;
        } else {
            parent.right = node;
            q.poll();
        }
        q.offer(node);
        return parent.val;
    }

    public TreeNode get_root() {
        return root;
    }
}
```

## 复杂度分析
- 初始化时间：\(O(n)\)
- 单次插入时间：\(O(1)\)
- 空间：\(O(n)\)（队列）

## 相关题目
- 102 二叉树的层序遍历
