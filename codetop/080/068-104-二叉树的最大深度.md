# 104. 二叉树的最大深度

- 题号：104
- 难度：简单
- LeetCode：[二叉树的最大深度](https://leetcode.cn/problems/maximum-depth-of-binary-tree/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：递归 DFS  
最大深度 = `1 + max(左子树深度, 右子树深度)`；空树深度为 0。

## Java 最佳实现

```java
class Solution {
    public int maxDepth(TreeNode root) {
        if (root == null) return 0;
        return 1 + Math.max(maxDepth(root.left), maxDepth(root.right));
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)（递归栈）

## 相关题目
- 111 二叉树的最小深度
- 110 平衡二叉树（需要高度信息）

## 总结（速记）
- 深度题基本都能一句话：`1 + max(left, right)`。
