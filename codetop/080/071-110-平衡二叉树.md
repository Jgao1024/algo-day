# 110. 平衡二叉树

- 题号：110
- 难度：简单
- LeetCode：[平衡二叉树](https://leetcode.cn/problems/balanced-binary-tree/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：后序 DFS 自底向上返回高度  
递归函数返回子树高度；如果发现不平衡直接返回 -1 向上传播。  
平衡条件：左右高度差不超过 1。

## Java 最佳实现

```java
class Solution {
    public boolean isBalanced(TreeNode root) {
        return height(root) != -1;
    }

    private int height(TreeNode node) {
        if (node == null) return 0;
        int lh = height(node.left);
        if (lh == -1) return -1;
        int rh = height(node.right);
        if (rh == -1) return -1;
        if (Math.abs(lh - rh) > 1) return -1;
        return 1 + Math.max(lh, rh);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)

## 相关题目
- 104 二叉树的最大深度
- 543 二叉树的直径（同样后序拿高度）

## 总结（速记）
- 自底向上：高度正常返回；一旦不平衡就返回 -1。
