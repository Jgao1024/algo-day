# 128. 最长连续序列

- 题号：128
- 难度：中等
- LeetCode：[最长连续序列](https://leetcode.cn/problems/longest-consecutive-sequence/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：HashSet 找“序列起点”  
把所有数放入集合。  
只从“没有前驱 num-1”的数开始向右扩展，统计长度，取最大值。

## Java 最佳实现

```java
import java.util.HashSet;
import java.util.Set;

class Solution {
    public int longestConsecutive(int[] nums) {
        Set<Integer> set = new HashSet<>();
        for (int x : nums) set.add(x);

        int ans = 0;
        for (int x : set) {
            if (set.contains(x - 1)) continue; // 不是起点
            int cur = x;
            int len = 1;
            while (set.contains(cur + 1)) {
                cur++;
                len++;
            }
            ans = Math.max(ans, len);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)（均摊）
- 空间：\(O(n)\)

## 相关题目
- 217 存在重复元素
- 219 存在重复元素 II

## 总结（速记）
- 只从“起点”出发扫，避免每个点都重复扩展。
