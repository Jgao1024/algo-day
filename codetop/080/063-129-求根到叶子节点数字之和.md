# 129. 求根到叶子节点数字之和

- 题号：129
- 难度：中等
- LeetCode：[求根到叶子节点数字之和](https://leetcode.cn/problems/sum-root-to-leaf-numbers/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：DFS 累加路径值  
从根往下走时维护当前数字 `cur = cur * 10 + node.val`。  
走到叶子结点就把 cur 加到答案里。

## Java 最佳实现

```java
class Solution {
    public int sumNumbers(TreeNode root) {
        return dfs(root, 0);
    }

    private int dfs(TreeNode node, int cur) {
        if (node == null) return 0;
        int next = cur * 10 + node.val;
        if (node.left == null && node.right == null) return next;
        return dfs(node.left, next) + dfs(node.right, next);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)（递归栈，h 为树高）

## 相关题目
- 112 路径总和
- 257 二叉树的所有路径

## 总结（速记）
- 一路把路径当成“十进制数”往下推：`cur = cur * 10 + val`，叶子就收割。
