# 394. 字符串解码

- 题号：394
- 难度：中等
- LeetCode：[字符串解码](https://leetcode.cn/problems/decode-string/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：双栈模拟  
遇到 `[`：把当前已构建的字符串、以及重复次数入栈，开始构建新段。  
遇到 `]`：弹出次数 k 和上一段字符串 prev，把当前段重复 k 次拼回去。  
字母直接追加；数字可能多位，要累积成 k。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Deque;

class Solution {
    public String decodeString(String s) {
        Deque<Integer> countStack = new ArrayDeque<>();
        Deque<StringBuilder> strStack = new ArrayDeque<>();
        StringBuilder cur = new StringBuilder();
        int k = 0;

        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            if (c >= '0' && c <= '9') {
                k = k * 10 + (c - '0');
            } else if (c == '[') {
                countStack.push(k);
                strStack.push(cur);
                k = 0;
                cur = new StringBuilder();
            } else if (c == ']') {
                int times = countStack.pop();
                StringBuilder prev = strStack.pop();
                for (int t = 0; t < times; t++) {
                    prev.append(cur);
                }
                cur = prev;
            } else {
                cur.append(c);
            }
        }

        return cur.toString();
    }
}
```

## 复杂度分析
- 时间：\(O(n + |ans|)\)（构建输出占主要）
- 空间：\(O(|ans|)\)

## 相关题目
- 20 有效的括号（栈）
- 227/224 基本计算器（表达式栈思路类似）

## 总结（速记）
- `[` 入栈保存“次数+上层字符串”；`]` 出栈做“重复+回拼”。
