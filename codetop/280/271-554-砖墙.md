# 554. 砖墙

- 题号：554
- 难度：中等
- LeetCode：[砖墙](https://leetcode.cn/problems/brick-wall/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 竖线尽量穿过“砖缝”。
- 统计每一行从左到右的前缀和位置（不包含最右边界），用哈希表计数；出现次数最多的缝位置就是能穿过最多行的地方。
- 答案 = 行数 - 最大缝次数。

## Java 最佳实现

```java
import java.util.HashMap;
import java.util.List;
import java.util.Map;

class Solution {
    public int leastBricks(List<List<Integer>> wall) {
        Map<Integer, Integer> cnt = new HashMap<>();
        int best = 0;
        for (List<Integer> row : wall) {
            int sum = 0;
            for (int i = 0; i < row.size() - 1; i++) {
                sum += row.get(i);
                int c = cnt.getOrDefault(sum, 0) + 1;
                cnt.put(sum, c);
                if (c > best) best = c;
            }
        }
        return wall.size() - best;
    }
}
```

## 复杂度分析
- 时间：\(O(\text{总砖块数})\)
- 空间：\(O(\text{不同缝位置数})\)

## 相关题目
- 560 和为 K 的子数组（前缀和 + 哈希）
