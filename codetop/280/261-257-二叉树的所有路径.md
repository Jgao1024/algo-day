# 257. 二叉树的所有路径

- 题号：257
- 难度：简单
- LeetCode：[二叉树的所有路径](https://leetcode.cn/problems/binary-tree-paths/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- DFS 前序遍历，把路径上的节点拼成 `a->b->c`。
- 到叶子节点就把当前路径加入答案；回溯时撤销本层追加的字符。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<String> binaryTreePaths(TreeNode root) {
        List<String> ans = new ArrayList<>();
        if (root == null) {
            return ans;
        }
        StringBuilder path = new StringBuilder();
        dfs(root, path, ans);
        return ans;
    }

    private void dfs(TreeNode node, StringBuilder path, List<String> ans) {
        int len = path.length();
        if (len != 0) {
            path.append("->");
        }
        path.append(node.val);

        if (node.left == null && node.right == null) {
            ans.add(path.toString());
        } else {
            if (node.left != null) {
                dfs(node.left, path, ans);
            }
            if (node.right != null) {
                dfs(node.right, path, ans);
            }
        }
        path.setLength(len);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)（每个节点访问一次）
- 空间：\(O(h)\)（递归栈，h 为树高；输出不计）

## 相关题目
- 112 路径总和
- 113 路径总和 II
- 257 二叉树的所有路径
