# 12. 整数转罗马数字

- 题号：12
- 难度：中等
- LeetCode：[整数转罗马数字](https://leetcode.cn/problems/integer-to-roman/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：贪心  
从大到小枚举罗马数字值，能减就减并追加对应字符串，直到 `num` 变为 0。

## Java 最佳实现

```java
class Solution {
    public String intToRoman(int num) {
        int[] vals = {1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1};
        String[] syms = {"M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"};

        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < vals.length; i++) {
            while (num >= vals[i]) {
                num -= vals[i];
                sb.append(syms[i]);
            }
        }
        return sb.toString();
    }
}
```

## 复杂度分析
- 时间：\(O(1)\)
- 空间：\(O(1)\)

## 相关题目
- 13 罗马数字转整数
