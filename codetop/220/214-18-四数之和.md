# 18. 四数之和

- 题号：18
- 难度：中等
- LeetCode：[四数之和](https://leetcode.cn/problems/4sum/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：排序 + 双指针  
先排序。固定前两个数 `i、j`，对剩余部分用左右指针 `l、r` 去找 `nums[l] + nums[r] = target - nums[i] - nums[j]`。  
过程中跳过重复值，避免重复答案。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class Solution {
    public List<List<Integer>> fourSum(int[] nums, int target) {
        Arrays.sort(nums);
        List<List<Integer>> ans = new ArrayList<>();
        int n = nums.length;

        for (int i = 0; i < n - 3; i++) {
            if (i > 0 && nums[i] == nums[i - 1]) {
                continue;
            }
            for (int j = i + 1; j < n - 2; j++) {
                if (j > i + 1 && nums[j] == nums[j - 1]) {
                    continue;
                }
                long need = (long) target - nums[i] - nums[j];
                int l = j + 1;
                int r = n - 1;
                while (l < r) {
                    long sum = (long) nums[l] + nums[r];
                    if (sum == need) {
                        ans.add(Arrays.asList(nums[i], nums[j], nums[l], nums[r]));
                        l++;
                        r--;
                        while (l < r && nums[l] == nums[l - 1]) {
                            l++;
                        }
                        while (l < r && nums[r] == nums[r + 1]) {
                            r--;
                        }
                    } else if (sum < need) {
                        l++;
                    } else {
                        r--;
                    }
                }
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n^3)\)
- 空间：\(O(1)\)（不计输出）

## 相关题目
- 15 三数之和
- 16 最接近的三数之和
