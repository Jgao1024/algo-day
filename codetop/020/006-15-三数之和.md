# 15. 三数之和

- 题号：15
- 难度：中等
- LeetCode：[三数之和](https://leetcode.cn/problems/3sum/)

## 题目描述
给你一个整数数组 `nums`，判断是否存在三个元素 `a,b,c` 使得 `a + b + c = 0`。

请你返回所有不重复的三元组 `[a,b,c]`（三元组内顺序不重要，结果集不允许重复）。

## 示例
- 示例1  
输入：`nums = [-1,0,1,2,-1,-4]`  
输出：`[[-1,-1,2],[-1,0,1]]`

- 示例2  
输入：`nums = [0,1,1]`  
输出：`[]`

- 示例3  
输入：`nums = [0,0,0]`  
输出：`[[0,0,0]]`

## 最佳解法思路
- 思路1（推荐）：排序 + 双指针  
先排序。枚举第一个数 `i`，然后用双指针 `l=i+1, r=n-1` 找 `nums[l] + nums[r] = -nums[i]`。  
去重关键：`i`、`l`、`r` 都要跳过重复值。

- 思路2：哈希（不推荐）  
固定一个数后做 TwoSum，但去重麻烦，通常比双指针更绕。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        Arrays.sort(nums);
        List<List<Integer>> ans = new ArrayList<>();
        int n = nums.length;

        for (int i = 0; i < n - 2; i++) {
            if (i > 0 && nums[i] == nums[i - 1]) continue;
            if (nums[i] > 0) break;

            int l = i + 1, r = n - 1;
            while (l < r) {
                int sum = nums[i] + nums[l] + nums[r];
                if (sum == 0) {
                    ans.add(Arrays.asList(nums[i], nums[l], nums[r]));
                    int lv = nums[l], rv = nums[r];
                    while (l < r && nums[l] == lv) l++;
                    while (l < r && nums[r] == rv) r--;
                } else if (sum < 0) {
                    l++;
                } else {
                    r--;
                }
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：排序 \(O(n\log n)\) + 双指针 \(O(n^2)\) => \(O(n^2)\)
- 空间：\(O(1)\)（不计输出）

## 相关题目
- 1 两数之和：哈希
- 167 两数之和 II：排序数组双指针
- 16 最接近的三数之和：同样排序 + 双指针
- 18 四数之和：多一层枚举
- 259 较小的三数之和：计数版（< target）

## 总结（速记）
- 三数/四数类题：先排序，再把高维问题降成“固定若干个数 + 双指针/二分”。
- 去重就记三句话：
  - 枚举位 `i` 跳过重复
  - 找到答案后 `l/r` 分别跳过重复
  - 能提前剪枝就剪：`nums[i] > 0` 直接结束
