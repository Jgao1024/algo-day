# 25. K 个一组翻转链表

- 题号：25
- 难度：困难
- LeetCode：[K 个一组翻转链表](https://leetcode.cn/problems/reverse-nodes-in-k-group/)

## 题目描述
给你一个链表，每 `k` 个节点一组进行翻转，请你返回翻转后的链表。

如果节点总数不是 `k` 的整数倍，那么最后剩余的节点保持原有顺序。

## 示例
- 示例1  
输入：`1->2->3->4->5`, `k = 2`  
输出：`2->1->4->3->5`

- 示例2  
输入：`1->2->3->4->5`, `k = 3`  
输出：`3->2->1->4->5`

## 最佳解法思路
- 思路1（推荐）：分段反转 + 连接（迭代）  
用 `dummy` 指向头，`groupPrev` 指向每段的前一个节点。每次先找到第 `k` 个节点 `kth`，若不足 `k` 直接结束；保存 `groupNext = kth.next`，把 `[groupPrev.next, kth]` 这一段原地反转；反转后把新段头尾与 `groupPrev/groupNext` 接回去，再移动 `groupPrev`。

- 思路2：递归  
先检查后面是否有 `k` 个节点；有则反转前 `k` 个，递归处理剩余并接上。实现更短，但递归有栈空间。

## Java 最佳实现

```java
class Solution {
    public ListNode reverseKGroup(ListNode head, int k) {
        ListNode dummy = new ListNode(0);
        dummy.next = head;

        ListNode groupPrev = dummy;
        while (true) {
            ListNode kth = getKth(groupPrev, k);
            if (kth == null) break;
            ListNode groupNext = kth.next;

            ListNode prev = groupNext;
            ListNode cur = groupPrev.next;
            while (cur != groupNext) {
                ListNode next = cur.next;
                cur.next = prev;
                prev = cur;
                cur = next;
            }

            ListNode newHead = kth;
            ListNode newTail = groupPrev.next;
            groupPrev.next = newHead;
            groupPrev = newTail;
        }

        return dummy.next;
    }

    private ListNode getKth(ListNode from, int k) {
        ListNode cur = from;
        for (int i = 0; i < k; i++) {
            cur = cur.next;
            if (cur == null) return null;
        }
        return cur;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 206 反转链表：基础整段反转
- 92 反转链表 II：区间反转
- 24 两两交换链表中的节点：k=2 的特例
- 143 重排链表：中点 + 反转 + 合并
- 61 旋转链表：断开/重连

## 总结（速记）
- 这题的难点是“每段找到边界 + 反转后正确接回去”。
- 反转一段时，把 `prev` 初始化为 `groupNext`，可以一次循环完成反转并自然把尾巴接到 `groupNext`。
