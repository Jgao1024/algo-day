# 501. 二叉搜索树中的众数

- 题号：501
- 难度：简单
- LeetCode：[二叉搜索树中的众数](https://leetcode.cn/problems/find-mode-in-binary-search-tree/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 中序遍历 BST 会得到非递减序列：
  - 统计当前值连续出现次数 `curCnt`
  - 维护全局最大次数 `maxCnt`
  - 若 `curCnt > maxCnt` 清空答案并加入当前值；若相等则追加

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    private Integer prev = null;
    private int curCnt = 0;
    private int maxCnt = 0;
    private final List<Integer> res = new ArrayList<>();

    public int[] findMode(TreeNode root) {
        dfs(root);
        int[] ans = new int[res.size()];
        for (int i = 0; i < res.size(); i++) ans[i] = res.get(i);
        return ans;
    }

    private void dfs(TreeNode node) {
        if (node == null) return;
        dfs(node.left);

        if (prev == null || node.val != prev) {
            prev = node.val;
            curCnt = 1;
        } else {
            curCnt++;
        }

        if (curCnt > maxCnt) {
            maxCnt = curCnt;
            res.clear();
            res.add(node.val);
        } else if (curCnt == maxCnt) {
            res.add(node.val);
        }

        dfs(node.right);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)（递归栈）

## 相关题目
- 98 验证二叉搜索树：BST 性质
- 530 二叉搜索树的最小绝对差：中序遍历
