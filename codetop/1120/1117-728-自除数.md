# 728. 自除数

- 题号：728
- 难度：简单
- LeetCode：[自除数](https://leetcode.cn/problems/self-dividing-numbers/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 枚举区间内每个数，按位检查：若包含 0 则失败；否则需要对每个数字 `d` 满足 `x % d == 0`。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<Integer> selfDividingNumbers(int left, int right) {
        List<Integer> ans = new ArrayList<>();
        for (int x = left; x <= right; x++) {
            if (ok(x)) ans.add(x);
        }
        return ans;
    }

    private boolean ok(int x) {
        int t = x;
        while (t > 0) {
            int d = t % 10;
            if (d == 0 || x % d != 0) return false;
            t /= 10;
        }
        return true;
    }
}
```

## 复杂度分析
- 时间：\(O((right-left+1)\cdot \log right)\)
- 空间：\(O(1)\)（不计返回列表）

## 相关题目
- 202 快乐数：同样是按位/循环处理
