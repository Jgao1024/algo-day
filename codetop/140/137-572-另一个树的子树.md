# 572. 另一个树的子树

- 题号：572
- 难度：简单
- LeetCode：[另一个树的子树](https://leetcode.cn/problems/subtree-of-another-tree/)

## 题目描述
判断 `subRoot` 是否为 `root` 的一棵子树（结构和值都要完全相同）。

## 示例
- 示例  
输入：`root = [3,4,5,1,2]`，`subRoot = [4,1,2]`  
输出：`true`

## 最佳解法思路
- 思路1（推荐）：遍历 + 同树判断  
遍历 `root` 的每个节点，把它当作候选根：  
若 `same(a,b)`（两棵树完全一致）为 true，则返回 true；否则继续在左右子树里找。

- 思路2：序列化 + KMP  
把树序列化（包含空节点标记）后做子串匹配。

## Java 最佳实现

```java
class Solution {
    public boolean isSubtree(TreeNode root, TreeNode subRoot) {
        if (subRoot == null) return true;
        if (root == null) return false;
        if (same(root, subRoot)) return true;
        return isSubtree(root.left, subRoot) || isSubtree(root.right, subRoot);
    }

    private boolean same(TreeNode a, TreeNode b) {
        if (a == null && b == null) return true;
        if (a == null || b == null) return false;
        if (a.val != b.val) return false;
        return same(a.left, b.left) && same(a.right, b.right);
    }
}
```

## 复杂度分析
- 时间：最坏 \(O(mn)\)
- 空间：\(O(h)\)（递归栈）

## 相关题目
- 100 相同的树：same 判断
- 剑指 Offer 26 树的子结构：匹配规则更宽松

## 总结（速记）
- 先“找可能的根”，再“整棵比对是否相同”。
