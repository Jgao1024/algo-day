# 补充题1. 排序奇升偶降链表

- 题号：补充题1
- 难度：中等
- 题面链接：[排序奇升偶降链表](https://mp.weixin.qq.com/s/0WVa2wIAeG0nYnVndZiEXQ)

## 题目描述
给定一个链表，**奇数位置**节点值是升序，**偶数位置**节点值是降序。要求把整个链表按升序排序。

## 示例
- 示例  
输入：`1 -> 8 -> 3 -> 6 -> 5 -> 4 -> 7 -> 2`  
（奇位：1,3,5,7 升序；偶位：8,6,4,2 降序）  
输出：`1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7 -> 8`

## 最佳解法思路
- 思路1（推荐）：拆分 + 反转 + 归并  
把链表按位置拆成两条：奇位链表（本来就升序）和偶位链表（降序）。  
把偶位链表反转成升序，然后把两条有序链表归并。

## Java 最佳实现

```java
class Solution {
    public ListNode sortOddAscEvenDescList(ListNode head) {
        if (head == null || head.next == null) return head;

        ListNode odd = head;
        ListNode even = head.next;
        ListNode evenHead = even;

        while (even != null && even.next != null) {
            odd.next = even.next;
            odd = odd.next;

            even.next = odd.next;
            even = even.next;
        }
        odd.next = null;

        ListNode evenAsc = reverse(evenHead);
        return merge(head, evenAsc);
    }

    private ListNode reverse(ListNode head) {
        ListNode pre = null;
        while (head != null) {
            ListNode next = head.next;
            head.next = pre;
            pre = head;
            head = next;
        }
        return pre;
    }

    private ListNode merge(ListNode a, ListNode b) {
        ListNode dummy = new ListNode(0);
        ListNode cur = dummy;
        while (a != null && b != null) {
            if (a.val <= b.val) {
                cur.next = a;
                a = a.next;
            } else {
                cur.next = b;
                b = b.next;
            }
            cur = cur.next;
        }
        cur.next = (a != null) ? a : b;
        return dummy.next;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 21 合并两个有序链表：归并
- 206 反转链表：反转偶位链表

## 总结（速记）
- “偶位降序”不难：拆出来反转成升序，再和奇位链表归并。
