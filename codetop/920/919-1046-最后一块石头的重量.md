# 1046. 最后一块石头的重量

- 题号：1046
- 难度：简单
- LeetCode：[最后一块石头的重量](https://leetcode.cn/problems/last-stone-weight/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 大根堆模拟：每次取出最大的两块 `x,y`，若 `x!=y` 则把 `x-y` 放回。

## Java 最佳实现

```java
import java.util.PriorityQueue;

class Solution {
    public int lastStoneWeight(int[] stones) {
        PriorityQueue<Integer> pq = new PriorityQueue<>((a, b) -> Integer.compare(b, a));
        for (int x : stones) pq.add(x);
        while (pq.size() >= 2) {
            int x = pq.poll();
            int y = pq.poll();
            if (x != y) pq.add(x - y);
        }
        return pq.isEmpty() ? 0 : pq.peek();
    }
}
```

## 复杂度分析
- 时间：\(O(n\log n)\)
- 空间：\(O(n)\)

## 相关题目
- 215 数组中的第K个最大元素：堆
