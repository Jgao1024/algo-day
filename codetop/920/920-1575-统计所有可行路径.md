# 1575. 统计所有可行路径

- 题号：1575
- 难度：困难
- LeetCode：[统计所有可行路径](https://leetcode.cn/problems/count-all-possible-routes/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 记忆化搜索：状态 `(pos, fuel)`，表示从 `pos` 出发、剩余油量为 `fuel` 的可行路径数。
- 转移：枚举下一个城市 `next`，消耗 `cost = |loc[pos]-loc[next]|`，若 `cost <= fuel` 则累加 `dfs(next, fuel-cost)`。
- 走到终点也算一条路径，所以每个状态先加上 `(pos == finish ? 1 : 0)`。

## Java 最佳实现

```java
import java.util.Arrays;

class Solution {
    private static final int MOD = 1_000_000_007;
    private int[] loc;
    private int finish;
    private int[][] memo;

    public int countRoutes(int[] locations, int start, int finish, int fuel) {
        this.loc = locations;
        this.finish = finish;
        memo = new int[locations.length][fuel + 1];
        for (int i = 0; i < locations.length; i++) {
            Arrays.fill(memo[i], -1);
        }
        return dfs(start, fuel);
    }

    private int dfs(int pos, int fuel) {
        if (memo[pos][fuel] != -1) return memo[pos][fuel];
        long ans = (pos == finish) ? 1 : 0;
        for (int next = 0; next < loc.length; next++) {
            if (next == pos) continue;
            int cost = Math.abs(loc[pos] - loc[next]);
            if (cost <= fuel) {
                ans += dfs(next, fuel - cost);
            }
        }
        ans %= MOD;
        memo[pos][fuel] = (int) ans;
        return memo[pos][fuel];
    }
}
```

## 复杂度分析
- 时间：\(O(n^2 \cdot fuel)\)
- 空间：\(O(n \cdot fuel)\)

## 相关题目
- 63 不同路径 II：计数 DP
