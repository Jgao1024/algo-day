# 400. 第N个数字

- 题号：400
- 难度：中等
- LeetCode：[第N个数字](https://leetcode.cn/problems/nth-digit/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：按位数分段定位  
1 位数有 9 个（1..9），贡献 9 位；  
2 位数有 90 个（10..99），贡献 180 位；  
3 位数有 900 个……  
先确定 n 落在哪个位数段，再定位到具体数字与该数字的第几位。

## Java 最佳实现

```java
class Solution {
    public int findNthDigit(int n) {
        long digit = 1;
        long start = 1;
        long count = 9;

        while (n > count) {
            n -= count;
            digit++;
            start *= 10;
            count = digit * start * 9;
        }

        long num = start + (n - 1) / digit;
        int idx = (int) ((n - 1) % digit);
        String s = String.valueOf(num);
        return s.charAt(idx) - '0';
    }
}
```

## 复杂度分析
- 时间：\(O(\log n)\)
- 空间：\(O(1)\)

## 相关题目
- 60 排列序列（分段定位的思路类似）

## 总结（速记）
- 先用“位数段”把 n 减到段内，再算：`num = start + (n-1)/digit`，`idx=(n-1)%digit`。