# 679. 24 点游戏

- 题号：679
- 难度：困难
- LeetCode：[24 点游戏](https://leetcode.cn/problems/24-game/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：回溯枚举两数运算（浮点容差）  
每次从数组里选两个数 a、b，生成所有可能结果：  
`a+b, a-b, b-a, a*b, a/b, b/a`（除法要判断分母不为 0）。  
把结果放回数组继续递归，直到只剩一个数，判断是否接近 24。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    private static final double EPS = 1e-6;

    public boolean judgePoint24(int[] cards) {
        List<Double> nums = new ArrayList<>();
        for (int x : cards) nums.add((double) x);
        return dfs(nums);
    }

    private boolean dfs(List<Double> nums) {
        if (nums.size() == 1) {
            return Math.abs(nums.get(0) - 24.0) < EPS;
        }

        int n = nums.size();
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                if (i == j) continue;
                List<Double> next = new ArrayList<>();
                for (int k = 0; k < n; k++) {
                    if (k != i && k != j) next.add(nums.get(k));
                }

                double a = nums.get(i), b = nums.get(j);
                if (i < j) { // 去重：加法乘法交换律
                    next.add(a + b);
                    if (dfs(next)) return true;
                    next.remove(next.size() - 1);

                    next.add(a * b);
                    if (dfs(next)) return true;
                    next.remove(next.size() - 1);
                }

                next.add(a - b);
                if (dfs(next)) return true;
                next.remove(next.size() - 1);

                if (Math.abs(b) > EPS) {
                    next.add(a / b);
                    if (dfs(next)) return true;
                    next.remove(next.size() - 1);
                }
            }
        }
        return false;
    }
}
```

## 复杂度分析
- 时间：常数级搜索（固定 4 张牌，枚举分支有限）
- 空间：递归深度常数级

## 相关题目
- 282 给表达式添加运算符（回溯枚举）

## 总结（速记）
- 每步拿两数做运算，把结果放回去继续搜；判断 24 用浮点容差。
