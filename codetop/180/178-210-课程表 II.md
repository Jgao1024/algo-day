# 210. 课程表 II

- 题号：210
- 难度：中等
- LeetCode：[课程表 II](https://leetcode.cn/problems/course-schedule-ii/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：拓扑排序（BFS/Kahn）  
统计每门课入度 indegree，入度为 0 的先入队。  
不断出队，把它指向的课入度减 1，变成 0 就入队。  
最终若能输出 n 门课，则该序列为答案，否则返回空数组。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Deque;
import java.util.List;

class Solution {
    public int[] findOrder(int numCourses, int[][] prerequisites) {
        List<List<Integer>> g = new ArrayList<>();
        for (int i = 0; i < numCourses; i++) g.add(new ArrayList<>());
        int[] indeg = new int[numCourses];
        for (int[] e : prerequisites) {
            int a = e[0], b = e[1]; // b -> a
            g.get(b).add(a);
            indeg[a]++;
        }

        Deque<Integer> q = new ArrayDeque<>();
        for (int i = 0; i < numCourses; i++) {
            if (indeg[i] == 0) q.addLast(i);
        }

        int[] order = new int[numCourses];
        int idx = 0;
        while (!q.isEmpty()) {
            int u = q.removeFirst();
            order[idx++] = u;
            for (int v : g.get(u)) {
                if (--indeg[v] == 0) q.addLast(v);
            }
        }

        if (idx != numCourses) return new int[0];
        return order;
    }
}
```

## 复杂度分析
- 时间：\(O(V+E)\)
- 空间：\(O(V+E)\)

## 相关题目
- 207 课程表（只判断是否有环）
- 802 找到最终的安全状态（有向图）

## 总结（速记）
- Kahn：**入度 0 入队**，出队就“删边减入度”，能出完就是无环。
