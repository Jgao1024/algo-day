# 169. 多数元素

## 题目描述
给定一个大小为 n 的数组，找到其中的多数元素。多数元素是指在数组中出现次数大于 ⌊n/2⌋ 的元素。

你可以假设数组是非空的，并且给定的数组总是存在多数元素。

## 解题思路
1. Boyer-Moore投票算法：
   - 维护一个候选众数candidate和计数器count
   - 遍历数组，如果当前元素等于candidate，count加1，否则减1
   - 当count为0时，更换candidate
   - 时间复杂度：O(n)，空间复杂度：O(1)

2. 排序法：
   - 将数组排序，中间元素一定是多数元素
   - 时间复杂度：O(nlogn)，空间复杂度：O(1)

## Java实现
```java
class Solution {
    public int majorityElement(int[] nums) {
        // Boyer-Moore投票算法
        int candidate = nums[0];
        int count = 1;
        
        for (int i = 1; i < nums.length; i++) {
            if (count == 0) {
                candidate = nums[i];
                count = 1;
            } else if (nums[i] == candidate) {
                count++;
            } else {
                count--;
            }
        }
        
        return candidate;
    }
}
```

## Python实现
```python
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        # Boyer-Moore投票算法
        candidate = nums[0]
        count = 1
        
        for num in nums[1:]:
            if count == 0:
                candidate = num
                count = 1
            elif num == candidate:
                count += 1
            else:
                count -= 1
                
        return candidate
```

## 复杂度分析
- 时间复杂度：O(n)，其中n是数组长度
- 空间复杂度：O(1)，只使用了两个变量

## 相关题目总结
1. 数组计数类题目：
   - 多数元素
   - 求众数 II
   - 数组中出现次数超过一半的数字
   - 统计元素出现次数

2. 解题技巧：
   - Boyer-Moore投票算法
   - 哈希表
   - 排序
   - 分治 