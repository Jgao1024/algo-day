# 209. 长度最小的子数组

- 题号：209
- 难度：中等
- LeetCode：[长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)

## 题目描述
给定正整数数组 `nums` 和正整数 `target`，返回和 \(\ge target\) 的最短连续子数组长度；不存在则返回 0。

## 示例
- 示例  
输入：`target = 7, nums = [2,3,1,2,4,3]`  
输出：`2`

## 最佳解法思路
- 思路1（推荐）：滑动窗口（双指针）  
右指针扩展窗口累加 `sum`，当 `sum >= target` 时尽量移动左指针缩小窗口并更新答案。

## Java 最佳实现

```java
class Solution {
    public int minSubArrayLen(int target, int[] nums) {
        int left = 0;
        int sum = 0;
        int ans = Integer.MAX_VALUE;
        for (int right = 0; right < nums.length; right++) {
            sum += nums[right];
            while (sum >= target) {
                ans = Math.min(ans, right - left + 1);
                sum -= nums[left++];
            }
        }
        return ans == Integer.MAX_VALUE ? 0 : ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 3 无重复字符的最长子串：同滑动窗口模板
- 713 乘积小于 K 的子数组：同双指针

## 总结（速记）
- 正数数组最短/最长子数组：优先想滑动窗口。 