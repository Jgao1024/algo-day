# 718. 最长重复子数组

## 题目描述
给两个整数数组 A 和 B ，返回两个数组中公共的、长度最长的子数组的长度。

## 解题思路
1. 动态规划法：
   - dp[i][j]表示以A[i-1]和B[j-1]结尾的最长公共子数组长度
   - 状态转移方程：dp[i][j] = dp[i-1][j-1] + 1 (if A[i-1] == B[j-1])
   - 时间复杂度：O(mn)，空间复杂度：O(mn)

2. 滑动窗口法：
   - 枚举所有可能的对齐方式
   - 计算每种对齐方式下的最长公共子数组
   - 时间复杂度：O((m+n)min(m,n))，空间复杂度：O(1)

## Java实现
```java
class Solution {
    public int findLength(int[] A, int[] B) {
        int m = A.length;
        int n = B.length;
        int[][] dp = new int[m + 1][n + 1];
        int maxLen = 0;
        
        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= n; j++) {
                if (A[i - 1] == B[j - 1]) {
                    dp[i][j] = dp[i - 1][j - 1] + 1;
                    maxLen = Math.max(maxLen, dp[i][j]);
                }
            }
        }
        
        return maxLen;
    }
}
```

## Python实现
```python
class Solution:
    def findLength(self, A: List[int], B: List[int]) -> int:
        m, n = len(A), len(B)
        dp = [[0] * (n + 1) for _ in range(m + 1)]
        max_len = 0
        
        for i in range(1, m + 1):
            for j in range(1, n + 1):
                if A[i - 1] == B[j - 1]:
                    dp[i][j] = dp[i - 1][j - 1] + 1
                    max_len = max(max_len, dp[i][j])
                    
        return max_len
```

## 复杂度分析
- 时间复杂度：O(mn)，其中m和n分别是数组A和B的长度
- 空间复杂度：O(mn)，需要存储dp数组

## 相关题目总结
1. 子数组类题目：
   - 最长重复子数组
   - 最长公共子序列
   - 最长递增子序列
   - 最大子数组和

2. 解题技巧：
   - 动态规划
   - 滑动窗口
   - 双指针
   - 字符串匹配 