# 179. 最大数

- 题号：179
- 难度：中等
- LeetCode：[最大数](https://leetcode.cn/problems/largest-number/)

## 题目描述
给定一组非负整数 `nums`，重新排列它们的顺序，使其连接起来组成的数字最大，返回该字符串。

## 示例
- 示例  
输入：`nums = [10,2]`  
输出：`"210"`

## 最佳解法思路
- 思路1（推荐）：自定义排序  
把数字转成字符串，排序规则：对 `a/b`，若 `b+a` 大于 `a+b`，则 `b` 应排在 `a` 前面。  
排序后拼接；如果最高位是 `0`，说明全是 0，返回 `"0"`。

## Java 最佳实现

```java
import java.util.Arrays;

class Solution {
    public String largestNumber(int[] nums) {
        String[] arr = new String[nums.length];
        for (int i = 0; i < nums.length; i++) arr[i] = String.valueOf(nums[i]);

        Arrays.sort(arr, (a, b) -> (b + a).compareTo(a + b));
        if (arr.length > 0 && "0".equals(arr[0])) return "0";

        StringBuilder sb = new StringBuilder();
        for (String s : arr) sb.append(s);
        return sb.toString();
    }
}
```

## 复杂度分析
- 时间：\(O(n\log n \cdot k)\)（k 为平均字符串长度）
- 空间：\(O(n)\)

## 相关题目
- 215 数组中的第K个最大元素：排序/选择问题
- 56 合并区间：同为“排序 + 规则”

## 总结（速记）
- 比较 `a+b` 和 `b+a` 决定先后；结果首位为 0 就直接 `"0"`。 
