# 010. 最长回文子串

## 题目描述
给你一个字符串 s，找到 s 中最长的回文子串。

- 输入：字符串 s
- 输出：最长回文子串

示例：
```
输入: s = "babad"
输出: "bab"
解释: "aba" 也是一个有效答案。
```

## 解题思路
1. 动态规划：
   - 定义 dp[i][j] 表示 s[i:j+1] 是否为回文串。
   - 状态转移：s[i]==s[j] 且 dp[i+1][j-1] 为真。
2. 中心扩展法：
   - 以每个字符或字符间隙为中心，向两边扩展。
   - 记录最长回文长度和起始位置。

## Java实现
```java
// 中心扩展法
class Solution {
    public String longestPalindrome(String s) {
        if (s == null || s.length() < 2) return s;
        int start = 0, maxLen = 1;
        for (int i = 0; i < s.length(); i++) {
            int len1 = expand(s, i, i);
            int len2 = expand(s, i, i + 1);
            int len = Math.max(len1, len2);
            if (len > maxLen) {
                start = i - (len - 1) / 2;
                maxLen = len;
            }
        }
        return s.substring(start, start + maxLen);
    }
    private int expand(String s, int left, int right) {
        while (left >= 0 && right < s.length() && s.charAt(left) == s.charAt(right)) {
            left--;
            right++;
        }
        return right - left - 1;
    }
}

// 动态规划
class Solution {
    public String longestPalindrome(String s) {
        int n = s.length();
        boolean[][] dp = new boolean[n][n];
        String res = "";
        for (int l = 0; l < n; l++) {
            for (int i = 0; i + l < n; i++) {
                int j = i + l;
                if (l == 0) dp[i][j] = true;
                else if (l == 1) dp[i][j] = (s.charAt(i) == s.charAt(j));
                else dp[i][j] = (s.charAt(i) == s.charAt(j)) && dp[i+1][j-1];
                if (dp[i][j] && l + 1 > res.length()) {
                    res = s.substring(i, j + 1);
                }
            }
        }
        return res;
    }
}
```

## Python实现
```python
# 中心扩展法
class Solution:
    def longestPalindrome(self, s: str) -> str:
        if len(s) < 2:
            return s
        start, max_len = 0, 1
        for i in range(len(s)):
            l1 = self.expand(s, i, i)
            l2 = self.expand(s, i, i+1)
            l = max(l1, l2)
            if l > max_len:
                start = i - (l - 1) // 2
                max_len = l
        return s[start:start+max_len]
    def expand(self, s, left, right):
        while left >= 0 and right < len(s) and s[left] == s[right]:
            left -= 1
            right += 1
        return right - left - 1

# 动态规划
class Solution:
    def longestPalindrome(self, s: str) -> str:
        n = len(s)
        dp = [[False]*n for _ in range(n)]
        res = ""
        for l in range(n):
            for i in range(n-l):
                j = i + l
                if l == 0:
                    dp[i][j] = True
                elif l == 1:
                    dp[i][j] = (s[i] == s[j])
                else:
                    dp[i][j] = (s[i] == s[j]) and dp[i+1][j-1]
                if dp[i][j] and l+1 > len(res):
                    res = s[i:j+1]
        return res
```

## 复杂度分析
- 时间复杂度：中心扩展O(n^2)，动态规划O(n^2)
- 空间复杂度：中心扩展O(1)，动态规划O(n^2)

## 相关题型总结
1. 回文串类题目：
   - 最长回文子串
   - 回文子序列
   - 分割回文串
2. 技巧总结：
   - 动态规划
   - 中心扩展
   - 双指针

---

> 回文串问题常考，中心扩展法和动态规划法都要熟练掌握。 