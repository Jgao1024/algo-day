# 008. 手撕快速排序

## 题目描述
实现快速排序算法，对给定的无序数组进行原地排序。

- 输入：一个整数数组 nums
- 输出：将 nums 原地排序

示例：
```
输入: [3,6,8,10,1,2,1]
输出: [1,1,2,3,6,8,10]
```

## 解题思路
快速排序是一种分治算法，核心思想如下：
1. 选择一个基准元素（pivot），通常选区间的第一个、最后一个或随机一个元素。
2. 通过一趟排序将数组分为两部分，左边都小于等于基准，右边都大于等于基准。
3. 递归地对左右两部分分别进行快速排序。

常见优化：
- 随机选取基准，防止极端情况下退化为O(n^2)
- 小区间用插入排序优化

## Java实现
```java
// 原地快速排序，递归实现
public class QuickSort {
    public void quickSort(int[] nums) {
        quickSort(nums, 0, nums.length - 1);
    }

    private void quickSort(int[] nums, int left, int right) {
        if (left >= right) return;
        // 随机选取基准，防止极端退化
        int pivotIdx = left + (int)(Math.random() * (right - left + 1));
        swap(nums, left, pivotIdx);
        int pivot = nums[left];
        int i = left, j = right;
        while (i < j) {
            while (i < j && nums[j] >= pivot) j--;
            while (i < j && nums[i] <= pivot) i++;
            if (i < j) swap(nums, i, j);
        }
        swap(nums, left, i); // 基准归位
        quickSort(nums, left, i - 1);
        quickSort(nums, i + 1, right);
    }

    private void swap(int[] nums, int i, int j) {
        int tmp = nums[i];
        nums[i] = nums[j];
        nums[j] = tmp;
    }
}
```

## Python实现
```python
# 原地快速排序，递归实现
import random

def quick_sort(nums, left, right):
    if left >= right:
        return
    # 随机选取基准，防止极端退化
    pivot_idx = random.randint(left, right)
    nums[left], nums[pivot_idx] = nums[pivot_idx], nums[left]
    pivot = nums[left]
    i, j = left, right
    while i < j:
        while i < j and nums[j] >= pivot:
            j -= 1
        while i < j and nums[i] <= pivot:
            i += 1
        if i < j:
            nums[i], nums[j] = nums[j], nums[i]
    nums[left], nums[i] = nums[i], nums[left]  # 基准归位
    quick_sort(nums, left, i - 1)
    quick_sort(nums, i + 1, right)

# 使用示例
nums = [3,6,8,10,1,2,1]
quick_sort(nums, 0, len(nums)-1)
print(nums)  # 输出: [1, 1, 2, 3, 6, 8, 10]
```

## 复杂度分析
- 平均时间复杂度：O(nlogn)
- 最坏时间复杂度：O(n^2)（已随机化基准可大大降低概率）
- 空间复杂度：O(logn)（递归栈空间）

## 相关题型总结
1. 排序类题目：
   - 快速排序
   - 归并排序
   - 堆排序
   - 冒泡排序
2. 技巧总结：
   - 分治思想
   - 双指针
   - 随机化防止退化
   - 原地排序

---

> 快速排序是面试和工程中最常用的排序算法之一，掌握其分治思想和优化技巧非常重要。 