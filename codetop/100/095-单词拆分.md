# 139. 单词拆分

## 题目描述
给定一个非空字符串 s 和一个包含非空单词的列表 wordDict，判定 s 是否可以被空格拆分为一个或多个在字典中出现的单词。

## 解题思路
1. 动态规划法：
   - dp[i]表示s[0...i-1]是否可以被拆分
   - 状态转移方程：dp[i] = dp[j] && wordDict.contains(s[j...i-1])
   - 时间复杂度：O(n²)，空间复杂度：O(n)

2. 记忆化搜索法：
   - 使用递归+记忆化搜索
   - 记录已经计算过的状态
   - 时间复杂度：O(n²)，空间复杂度：O(n)

## Java实现
```java
class Solution {
    public boolean wordBreak(String s, List<String> wordDict) {
        Set<String> wordSet = new HashSet<>(wordDict);
        int n = s.length();
        boolean[] dp = new boolean[n + 1];
        dp[0] = true;
        
        for (int i = 1; i <= n; i++) {
            for (int j = 0; j < i; j++) {
                if (dp[j] && wordSet.contains(s.substring(j, i))) {
                    dp[i] = true;
                    break;
                }
            }
        }
        
        return dp[n];
    }
}
```

## Python实现
```python
class Solution:
    def wordBreak(self, s: str, wordDict: List[str]) -> bool:
        word_set = set(wordDict)
        n = len(s)
        dp = [False] * (n + 1)
        dp[0] = True
        
        for i in range(1, n + 1):
            for j in range(i):
                if dp[j] and s[j:i] in word_set:
                    dp[i] = True
                    break
                    
        return dp[n]
```

## 复杂度分析
- 时间复杂度：O(n²)，其中n是字符串长度
- 空间复杂度：O(n)，需要存储dp数组和wordDict集合

## 相关题目总结
1. 字符串拆分类题目：
   - 单词拆分
   - 单词拆分 II
   - 分割回文串
   - 复原IP地址

2. 解题技巧：
   - 动态规划
   - 记忆化搜索
   - 字符串处理
   - 哈希表 