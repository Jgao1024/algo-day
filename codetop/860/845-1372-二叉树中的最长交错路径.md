# 1372. 二叉树中的最长交错路径

- 题号：1372
- 难度：中等
- LeetCode：[二叉树中的最长交错路径](https://leetcode.cn/problems/longest-zigzag-path-in-a-binary-tree/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- DFS：对每个节点，分别维护“最后一步往左/往右”时的最长长度：
  - 从父到子：
    - 走到左孩子：下一步方向变为左，所以长度 = 右方向长度 + 1
    - 走到右孩子：长度 = 左方向长度 + 1
  - 全局取最大

## Java 最佳实现

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    private int ans = 0;

    public int longestZigZag(TreeNode root) {
        if (root == null) return 0;
        dfs(root.left, true, 1);
        dfs(root.right, false, 1);
        return ans;
    }

    // isLeft 表示上一步是否走向左子节点
    private void dfs(TreeNode node, boolean isLeft, int len) {
        if (node == null) return;
        ans = Math.max(ans, len);
        if (isLeft) {
            dfs(node.right, false, len + 1);
            dfs(node.left, true, 1);
        } else {
            dfs(node.left, true, len + 1);
            dfs(node.right, false, 1);
        }
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)

## 相关题目
- 543 二叉树的直径：树上路径
- 124 二叉树中的最大路径和：树 DFS
