# 185. 部门工资前三高的所有员工

- 题号：185
- 难度：困难
- LeetCode：[部门工资前三高的所有员工](https://leetcode.cn/problems/department-top-three-salaries/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- SQL：对每个部门按工资降序做去重排名（Dense Rank），取排名 <= 3 的记录，再关联部门表输出。

## Java 最佳实现

```java
// 这题提交 SQL
/*
SELECT d.Name AS Department, e.Name AS Employee, e.Salary AS Salary
FROM (
    SELECT e.*,
           DENSE_RANK() OVER (PARTITION BY e.DepartmentId ORDER BY e.Salary DESC) AS rk
    FROM Employee e
) e
JOIN Department d ON d.Id = e.DepartmentId
WHERE e.rk <= 3;
*/
```

## 复杂度分析
- 时间：取决于数据库执行计划（一般为排序/窗口函数）
- 空间：取决于数据库执行计划

## 相关题目
- 184 部门工资最高的员工：窗口函数/子查询
- 178 分数排名：DENSE_RANK
