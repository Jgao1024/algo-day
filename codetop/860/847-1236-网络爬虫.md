# 1236. 网络爬虫

- 题号：1236
- 难度：中等
- LeetCode：[网络爬虫](https://leetcode.cn/problems/web-crawler/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- BFS/DFS：
  - 只爬取与 `startUrl` 同域名的页面
  - 用 `visited` 去重
  - 每访问一个 URL 就调用 `HtmlParser.getUrls(url)` 拿到邻接 URL

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Deque;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

/**
 * // This is the HtmlParser's API interface.
 * // You should not implement it, or speculate about its implementation
 * interface HtmlParser {
 *     public List<String> getUrls(String url);
 * }
 */
class Solution {
    public List<String> crawl(String startUrl, HtmlParser htmlParser) {
        String host = getHost(startUrl);
        Set<String> vis = new HashSet<>();
        Deque<String> q = new ArrayDeque<>();
        vis.add(startUrl);
        q.offer(startUrl);

        while (!q.isEmpty()) {
            String cur = q.poll();
            for (String nxt : htmlParser.getUrls(cur)) {
                if (!host.equals(getHost(nxt))) continue;
                if (vis.add(nxt)) q.offer(nxt);
            }
        }
        return new ArrayList<>(vis);
    }

    private String getHost(String url) {
        int i = url.indexOf("://");
        int start = (i == -1) ? 0 : i + 3;
        int slash = url.indexOf('/', start);
        return (slash == -1) ? url.substring(start) : url.substring(start, slash);
    }
}
```

## 复杂度分析
- 时间：与可访问的同域页面数量及链接数有关
- 空间：\(O(V)\)

## 相关题目
- 841 钥匙和房间：图遍历
- 133 克隆图：图遍历
