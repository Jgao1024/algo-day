# 1408. 数组中的字符串匹配

- 题号：1408
- 难度：简单
- LeetCode：[数组中的字符串匹配](https://leetcode.cn/problems/string-matching-in-an-array/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 暴力判断子串：
  - 对每个 `words[i]`，看它是否是某个 `words[j]`（`i!=j`）的子串
  - 是则加入答案

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<String> stringMatching(String[] words) {
        List<String> ans = new ArrayList<>();
        for (int i = 0; i < words.length; i++) {
            for (int j = 0; j < words.length; j++) {
                if (i == j) continue;
                if (words[j].contains(words[i])) {
                    ans.add(words[i]);
                    break;
                }
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n^2 \cdot L)\)（与 contains 实现相关）
- 空间：\(O(1)\)（不算输出）

## 相关题目
- 28 找出字符串中第一个匹配项的下标：匹配
- 459 重复的子字符串：字符串
