# 1490. 克隆 N 叉树

- 题号：1490
- 难度：中等
- LeetCode：[克隆 N 叉树](https://leetcode.cn/problems/clone-n-ary-tree/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- DFS + 哈希表（推荐）  
用 `map` 记录“原节点 -> 克隆节点”，避免重复克隆。  
递归克隆当前节点，然后克隆所有子节点并加入 `children`。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.HashMap;
import java.util.Map;

class Solution {
    private final Map<Node, Node> map = new HashMap<>();

    public Node cloneTree(Node root) {
        if (root == null) return null;
        Node cached = map.get(root);
        if (cached != null) return cached;

        Node copy = new Node(root.val, new ArrayList<>());
        map.put(root, copy);
        for (Node child : root.children) {
            copy.children.add(cloneTree(child));
        }
        return copy;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 133 克隆图

## 总结（速记）
- **树也可用“克隆图”的套路：map 记忆化**。
