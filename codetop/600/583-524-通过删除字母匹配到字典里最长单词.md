# 524. 通过删除字母匹配到字典里最长单词

- 题号：524
- 难度：中等
- LeetCode：[通过删除字母匹配到字典里最长单词](https://leetcode.cn/problems/longest-word-in-dictionary-through-deleting/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 贪心匹配子序列（推荐）  
遍历字典里的每个单词，判断它是否是 `s` 的子序列（双指针）。  
用长度优先，长度相同取字典序更小的作为答案。

## Java 最佳实现

```java
import java.util.List;

class Solution {
    public String findLongestWord(String s, List<String> dictionary) {
        String best = "";
        for (String w : dictionary) {
            if (!isSubsequence(s, w)) continue;
            if (w.length() > best.length() || (w.length() == best.length() && w.compareTo(best) < 0)) {
                best = w;
            }
        }
        return best;
    }

    private boolean isSubsequence(String s, String w) {
        int i = 0, j = 0;
        while (i < s.length() && j < w.length()) {
            if (s.charAt(i) == w.charAt(j)) j++;
            i++;
        }
        return j == w.length();
    }
}
```

## 复杂度分析
- 时间：\(O(\sum (|s| + |word|))\)
- 空间：\(O(1)\)

## 相关题目
- 392 判断子序列

## 总结（速记）
- **逐个单词做子序列匹配，按“最长/字典序”更新答案**。
