# 992. K 个不同整数的子数组

- 题号：992
- 难度：困难
- LeetCode：[K 个不同整数的子数组](https://leetcode.cn/problems/subarrays-with-k-different-integers/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 转化：exactly(K) = atMost(K) - atMost(K-1)（推荐）  
`atMost(K)`：滑动窗口统计“窗口内不同数 <= K”的子数组个数。  
右指针扩张，若不同数超 K，就移动左指针收缩；对每个 `right`，贡献为 `right-left+1`。

## Java 最佳实现

```java
import java.util.HashMap;
import java.util.Map;

class Solution {
    public int subarraysWithKDistinct(int[] nums, int k) {
        return atMost(nums, k) - atMost(nums, k - 1);
    }

    private int atMost(int[] nums, int k) {
        if (k <= 0) return 0;
        Map<Integer, Integer> cnt = new HashMap<>();
        int left = 0;
        int distinct = 0;
        int ans = 0;
        for (int right = 0; right < nums.length; right++) {
            int x = nums[right];
            int c = cnt.getOrDefault(x, 0);
            if (c == 0) distinct++;
            cnt.put(x, c + 1);

            while (distinct > k) {
                int y = nums[left++];
                int cy = cnt.get(y) - 1;
                if (cy == 0) {
                    cnt.remove(y);
                    distinct--;
                } else {
                    cnt.put(y, cy);
                }
            }
            ans += right - left + 1;
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(K)\)

## 相关题目
- 992 同题
- 340 至多包含 K 个不同字符的最长子串

## 总结（速记）
- **`exactlyK = atMostK - atMost(K-1)`**。
