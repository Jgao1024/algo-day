# 1410. HTML 实体解析器

- 题号：1410
- 难度：中等
- LeetCode：[HTML 实体解析器](https://leetcode.cn/problems/html-entity-parser/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 线性扫描替换（推荐）  
遇到 `&` 时，尝试匹配 6 种固定实体：  
`&quot; &apos; &amp; &gt; &lt; &frasl;`，匹配成功就替换并跳过；否则原样输出。

## Java 最佳实现

```java
class Solution {
    public String entityParser(String text) {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < text.length(); i++) {
            char c = text.charAt(i);
            if (c != '&') {
                sb.append(c);
                continue;
            }
            if (text.startsWith("&quot;", i)) {
                sb.append('"');
                i += 5;
            } else if (text.startsWith("&apos;", i)) {
                sb.append('\'');
                i += 5;
            } else if (text.startsWith("&amp;", i)) {
                sb.append('&');
                i += 4;
            } else if (text.startsWith("&gt;", i)) {
                sb.append('>');
                i += 3;
            } else if (text.startsWith("&lt;", i)) {
                sb.append('<');
                i += 3;
            } else if (text.startsWith("&frasl;", i)) {
                sb.append('/');
                i += 6;
            } else {
                sb.append('&');
            }
        }
        return sb.toString();
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 722 删除注释（扫描解析）

## 总结（速记）
- **只要 6 个固定实体，直接 `startsWith` 匹配替换**。
