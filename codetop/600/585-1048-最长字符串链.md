# 1048. 最长字符串链

- 题号：1048
- 难度：中等
- LeetCode：[最长字符串链](https://leetcode.cn/problems/longest-string-chain/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- DP + HashMap（推荐）  
按单词长度升序排序。对每个单词 `w`，枚举删掉某一位得到前驱 `pre`，则  
`dp[w] = max(dp[pre] + 1)`，若不存在前驱则为 1。最终取最大值。

## Java 最佳实现

```java
import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;

class Solution {
    public int longestStrChain(String[] words) {
        Arrays.sort(words, (a, b) -> a.length() - b.length());
        Map<String, Integer> dp = new HashMap<>();
        int ans = 0;
        for (String w : words) {
            int best = 1;
            for (int i = 0; i < w.length(); i++) {
                String pre = w.substring(0, i) + w.substring(i + 1);
                Integer v = dp.get(pre);
                if (v != null) best = Math.max(best, v + 1);
            }
            dp.put(w, best);
            ans = Math.max(ans, best);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n \cdot L^2)\)
- 空间：\(O(n)\)

## 相关题目
- 300 最长递增子序列

## 总结（速记）
- **长度排序 + “删 1 个字符找前驱”做 DP**。
