# 166. 分数到小数

- 题号：166
- 难度：中等
- LeetCode：[分数到小数](https://leetcode.cn/problems/fraction-to-recurring-decimal/)

## 题目描述
给定分子 `numerator` 和分母 `denominator`，以字符串形式返回分数的小数表示。  
若小数部分为循环小数，则将循环节用括号括起来。

## 示例
- 示例1  
输入：`numerator = 1, denominator = 2`  
输出：`"0.5"`

- 示例2  
输入：`numerator = 2, denominator = 3`  
输出：`"0.(6)"`

## 最佳解法思路
- 思路1（推荐）：模拟长除法 + 余数位置哈希表  
整数部分直接除。小数部分每次用余数 `rem`：  
`rem *= 10`，当前位是 `rem / den`，新余数 `rem % den`。  
如果某个余数重复出现，说明从第一次出现的位置开始循环。

## Java 最佳实现

```java
import java.util.HashMap;
import java.util.Map;

class Solution {
    public String fractionToDecimal(int numerator, int denominator) {
        if (numerator == 0) return "0";
        StringBuilder sb = new StringBuilder();

        long n = numerator;
        long d = denominator;
        if ((n < 0) ^ (d < 0)) sb.append('-');
        n = Math.abs(n);
        d = Math.abs(d);

        sb.append(n / d);
        long rem = n % d;
        if (rem == 0) return sb.toString();

        sb.append('.');
        Map<Long, Integer> pos = new HashMap<>();
        while (rem != 0) {
            if (pos.containsKey(rem)) {
                int idx = pos.get(rem);
                sb.insert(idx, '(');
                sb.append(')');
                break;
            }
            pos.put(rem, sb.length());
            rem *= 10;
            sb.append(rem / d);
            rem %= d;
        }
        return sb.toString();
    }
}
```

## 复杂度分析
- 时间：\(O(L)\)（L 为小数位长度，至多分母大小级别）
- 空间：\(O(L)\)

## 相关题目
- 7 整数反转：注意溢出/符号
- 67 二进制求和：同“模拟运算”

## 总结（速记）
- 循环小数本质是“余数重复”；用 map 记余数第一次出现的位置即可插括号。 