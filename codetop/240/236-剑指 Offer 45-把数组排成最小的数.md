# 剑指 Offer 45. 把数组排成最小的数

- 题号：剑指 Offer 45
- 难度：中等
- LeetCode：[把数组排成最小的数](https://leetcode.cn/problems/ba-shu-zu-pai-cheng-zui-xiao-de-shu-lcof/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：自定义排序  
把每个数字转成字符串，对任意两个字符串 `a,b`：  
若 `a+b` 字典序小于 `b+a`，则 `a` 应排在 `b` 前。按此规则排序后拼接就是最小结果。

## Java 最佳实现

```java
import java.util.Arrays;

class Solution {
    public String minNumber(int[] nums) {
        String[] a = new String[nums.length];
        for (int i = 0; i < nums.length; i++) {
            a[i] = String.valueOf(nums[i]);
        }

        Arrays.sort(a, (x, y) -> (x + y).compareTo(y + x));

        StringBuilder sb = new StringBuilder();
        for (String s : a) {
            sb.append(s);
        }
        return sb.toString();
    }
}
```

## 复杂度分析
- 时间：\(O(n \log n \cdot k)\)（k 为平均字符串长度）
- 空间：\(O(nk)\)

## 相关题目
- 179 最大数
