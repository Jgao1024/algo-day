# 6. Z 字形变换

- 题号：6
- 难度：中等
- LeetCode：[Z 字形变换](https://leetcode.cn/problems/zigzag-conversion/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：按行模拟  
准备 `numRows` 个 `StringBuilder` 作为每一行的缓冲区。用一个指针 `r` 表示当前行，方向在到达首/尾行时反转，把字符依次追加到对应行，最后拼接所有行即可。

## Java 最佳实现

```java
class Solution {
    public String convert(String s, int numRows) {
        if (numRows <= 1 || s.length() <= numRows) {
            return s;
        }

        StringBuilder[] rows = new StringBuilder[numRows];
        for (int i = 0; i < numRows; i++) {
            rows[i] = new StringBuilder();
        }

        int r = 0;
        int dir = 1;
        for (int i = 0; i < s.length(); i++) {
            rows[r].append(s.charAt(i));
            if (r == 0) {
                dir = 1;
            } else if (r == numRows - 1) {
                dir = -1;
            }
            r += dir;
        }

        StringBuilder ans = new StringBuilder();
        for (StringBuilder sb : rows) {
            ans.append(sb);
        }
        return ans.toString();
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 68 文本左右对齐
