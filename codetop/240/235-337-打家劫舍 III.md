# 337. 打家劫舍 III

- 题号：337
- 难度：中等
- LeetCode：[打家劫舍 III](https://leetcode.cn/problems/house-robber-iii/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：树形 DP  
对每个节点返回两个值：  
- `notRob`：不偷当前节点时，子树能偷到的最大值  
- `rob`：偷当前节点时，子树能偷到的最大值  
转移：  
`rob = node.val + left.notRob + right.notRob`  
`notRob = max(left.rob,left.notRob) + max(right.rob,right.notRob)`

## Java 最佳实现

```java
class Solution {
    public int rob(TreeNode root) {
        int[] res = dfs(root);
        return Math.max(res[0], res[1]);
    }

    private int[] dfs(TreeNode node) {
        if (node == null) {
            return new int[]{0, 0};
        }
        int[] left = dfs(node.left);
        int[] right = dfs(node.right);

        int notRob = Math.max(left[0], left[1]) + Math.max(right[0], right[1]);
        int rob = node.val + left[0] + right[0];
        return new int[]{notRob, rob};
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)（递归栈）

## 相关题目
- 198 打家劫舍
- 213 打家劫舍 II
