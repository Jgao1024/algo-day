# 486. 预测赢家

- 题号：486
- 难度：中等
- LeetCode：[预测赢家](https://leetcode.cn/problems/predict-the-winner/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：区间 DP（分差）  
定义 `dp[i][j]` 为在区间 [i..j] 内，当前玩家相对另一个玩家的“最大分差”。  
转移：  
`dp[i][j] = max(nums[i] - dp[i+1][j], nums[j] - dp[i][j-1])`  
最终 `dp[0][n-1] >= 0` 则先手不输。  
可用一维滚动优化。

## Java 最佳实现

```java
class Solution {
    public boolean predictTheWinner(int[] nums) {
        int n = nums.length;
        int[] dp = new int[n];
        for (int i = 0; i < n; i++) dp[i] = nums[i];
        for (int len = 2; len <= n; len++) {
            for (int i = 0; i + len - 1 < n; i++) {
                int j = i + len - 1;
                dp[i] = Math.max(nums[i] - dp[i + 1], nums[j] - dp[i]);
            }
        }
        return dp[0] >= 0;
    }
}
```

## 复杂度分析
- 时间：\(O(n^2)\)
- 空间：\(O(n)\)

## 相关题目
- 877 石子游戏（同类博弈 DP）

## 总结（速记）
- 博弈题常用“分差 dp”：拿左/拿右，减去对手在剩余区间的最优分差。