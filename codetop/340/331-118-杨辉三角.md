# 118. 杨辉三角

- 题号：118
- 难度：简单
- LeetCode：[杨辉三角](https://leetcode.cn/problems/pascals-triangle/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：逐行构造  
第 i 行长度 i+1，首尾为 1，中间为上一行相邻两数之和。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<List<Integer>> generate(int numRows) {
        List<List<Integer>> ans = new ArrayList<>();
        for (int i = 0; i < numRows; i++) {
            List<Integer> row = new ArrayList<>();
            for (int j = 0; j <= i; j++) {
                if (j == 0 || j == i) row.add(1);
                else row.add(ans.get(i - 1).get(j - 1) + ans.get(i - 1).get(j));
            }
            ans.add(row);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n^2)\)
- 空间：\(O(n^2)\)

## 相关题目
- 119 杨辉三角 II

## 总结（速记）
- 首尾永远 1，中间 = 上一行左上 + 右上。