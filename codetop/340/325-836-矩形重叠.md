# 836. 矩形重叠

- 题号：836
- 难度：简单
- LeetCode：[矩形重叠](https://leetcode.cn/problems/rectangle-overlap/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：判断是否“有正面积交集”  
两个矩形不重叠当且仅当在 x 或 y 方向上“完全分离”。  
重叠条件：  
`min(x2, x4) > max(x1, x3)` 且 `min(y2, y4) > max(y1, y3)`。

## Java 最佳实现

```java
class Solution {
    public boolean isRectangleOverlap(int[] rec1, int[] rec2) {
        int x1 = rec1[0], y1 = rec1[1], x2 = rec1[2], y2 = rec1[3];
        int x3 = rec2[0], y3 = rec2[1], x4 = rec2[2], y4 = rec2[3];
        return Math.min(x2, x4) > Math.max(x1, x3) && Math.min(y2, y4) > Math.max(y1, y3);
    }
}
```

## 复杂度分析
- 时间：\(O(1)\)
- 空间：\(O(1)\)

## 相关题目
- 223 矩形面积

## 总结（速记）
- 要“正面积重叠”：用严格大于（`>`），边贴边不算重叠。