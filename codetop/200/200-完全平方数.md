# 279. 完全平方数

## 题目描述
给定正整数 n，找到若干个完全平方数（比如 1, 4, 9, 16, ...）使得它们的和等于 n。你需要让组成和的完全平方数的个数最少。

示例 1:
```
输入：n = 12
输出：3 
解释：12 = 4 + 4 + 4
```

示例 2:
```
输入：n = 13
输出：2
解释：13 = 4 + 9
```

## 解题思路
1. 动态规划方法：
   - dp[i]表示组成数字i的最少完全平方数个数
   - 对于每个数字i，遍历所有可能的完全平方数j*j
   - dp[i] = min(dp[i], dp[i-j*j] + 1)
   - 时间复杂度：O(n*sqrt(n))，空间复杂度：O(n)

2. 关键点：
   - 初始化dp数组
   - 处理边界情况
   - 优化遍历范围

## Java实现
```java
class Solution {
    public int numSquares(int n) {
        int[] dp = new int[n + 1];
        Arrays.fill(dp, Integer.MAX_VALUE);
        dp[0] = 0;
        
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j * j <= i; j++) {
                dp[i] = Math.min(dp[i], dp[i - j * j] + 1);
            }
        }
        
        return dp[n];
    }
}
```

## Python实现
```python
class Solution:
    def numSquares(self, n: int) -> int:
        dp = [float('inf')] * (n + 1)
        dp[0] = 0
        
        for i in range(1, n + 1):
            j = 1
            while j * j <= i:
                dp[i] = min(dp[i], dp[i - j * j] + 1)
                j += 1
                
        return dp[n]
```

## 复杂度分析
- 时间复杂度：O(n*sqrt(n))，其中n是给定的正整数
- 空间复杂度：O(n)，需要存储dp数组

## 相关题目总结
1. 动态规划类题目：
   - 完全平方数
   - 零钱兑换
   - 爬楼梯
   - 组合总和

2. 解题技巧：
   - 使用动态规划
   - 优化遍历范围
   - 处理边界情况
   - 注意初始化 