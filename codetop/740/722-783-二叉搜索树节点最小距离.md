# 783. 二叉搜索树节点最小距离

- 题号：783
- 难度：简单
- LeetCode：[二叉搜索树节点最小距离](https://leetcode.cn/problems/minimum-distance-between-bst-nodes/)

## 题目描述
给定二叉搜索树，返回任意两个不同节点值之间的最小差值。

## 示例
输入：`[4,2,6,1,3]`，输出：`1`

## 最佳解法思路
- BST 中序遍历是递增序列，相邻元素差值最小。
- 中序遍历过程中记录上一个节点值 `prev`，更新答案 `min(cur-prev)`。

## Java 最佳实现

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    private Integer prev = null;
    private int ans = Integer.MAX_VALUE;

    public int minDiffInBST(TreeNode root) {
        inorder(root);
        return ans;
    }

    private void inorder(TreeNode node) {
        if (node == null) {
            return;
        }
        inorder(node.left);
        if (prev != null) {
            ans = Math.min(ans, node.val - prev);
        }
        prev = node.val;
        inorder(node.right);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)

## 相关题目
- 530 二叉搜索树的最小绝对差（同题）
