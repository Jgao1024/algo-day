# 406. 根据身高重建队列

- 题号：406
- 难度：中等
- LeetCode：[根据身高重建队列](https://leetcode.cn/problems/queue-reconstruction-by-height/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 先按身高降序、k 升序排序；然后按顺序把每个人插入到结果列表的下标 `k` 位置。
- 因为先放高个子，插入时不会影响已放好的“前面有多少个不矮于他的人”。

## Java 最佳实现

```java
import java.util.Arrays;
import java.util.LinkedList;
import java.util.List;

class Solution {
    public int[][] reconstructQueue(int[][] people) {
        Arrays.sort(people, (a, b) -> {
            if (a[0] != b[0]) return Integer.compare(b[0], a[0]);
            return Integer.compare(a[1], b[1]);
        });

        List<int[]> list = new LinkedList<>();
        for (int[] p : people) {
            list.add(p[1], p);
        }

        int[][] res = new int[people.length][2];
        for (int i = 0; i < list.size(); i++) {
            res[i] = list.get(i);
        }
        return res;
    }
}
```

## 复杂度分析
- 时间：排序 \(O(n\log n)\)，插入最坏 \(O(n^2)\)
- 空间：\(O(n)\)

## 相关题目
- 1353 最多可以参加的会议数目：排序 + 贪心
