# 881. 救生艇

- 题号：881
- 难度：中等
- LeetCode：[救生艇](https://leetcode.cn/problems/boats-to-save-people/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 贪心：排序后用双指针，最重的人尽量和最轻的人同船。
- 若 `light + heavy <= limit`，两人一船；否则重的单独一船。

## Java 最佳实现

```java
import java.util.Arrays;

class Solution {
    public int numRescueBoats(int[] people, int limit) {
        Arrays.sort(people);
        int i = 0, j = people.length - 1;
        int ans = 0;
        while (i <= j) {
            if (people[i] + people[j] <= limit) i++;
            j--;
            ans++;
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n\log n)\)
- 空间：\(O(1)\)

## 相关题目
- 15 三数之和：排序 + 双指针
