# 剑指 Offer 47. 礼物的最大价值

- 题号：剑指 Offer 47
- 难度：中等
- LeetCode：[礼物的最大价值](https://leetcode.cn/problems/li-wu-de-zui-da-jie-zhi-lcof/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- DP：每个格子只能从上或左过来。
- `dp[j]` 表示当前行到第 `j` 列的最大价值：`dp[j] = max(dp[j], dp[j-1]) + grid[i][j]`。

## Java 最佳实现

```java
class Solution {
    public int maxValue(int[][] grid) {
        int m = grid.length;
        int n = grid[0].length;

        int[] dp = new int[n];
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                int up = dp[j];
                int left = j > 0 ? dp[j - 1] : 0;
                dp[j] = Math.max(up, left) + grid[i][j];
            }
        }
        return dp[n - 1];
    }
}
```

## 复杂度分析
- 时间：\(O(mn)\)
- 空间：\(O(n)\)

## 相关题目
- 64 最小路径和：同样是网格 DP
