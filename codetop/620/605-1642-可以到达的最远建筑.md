# 1642. 可以到达的最远建筑

- 题号：1642
- 难度：中等
- LeetCode：[可以到达的最远建筑](https://leetcode.cn/problems/furthest-building-you-can-reach/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 贪心 + 小根堆：把所有需要爬升的高度差都先当作“用梯子”，放进小根堆。
- 如果堆大小超过梯子数量，说明有一次爬升必须改用砖，把堆里最小的爬升弹出用砖支付；砖不够就停在当前位置。

## Java 最佳实现

```java
import java.util.PriorityQueue;

class Solution {
    public int furthestBuilding(int[] heights, int bricks, int ladders) {
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        for (int i = 0; i < heights.length - 1; i++) {
            int diff = heights[i + 1] - heights[i];
            if (diff <= 0) continue;

            pq.add(diff);
            if (pq.size() > ladders) {
                bricks -= pq.poll();
                if (bricks < 0) return i;
            }
        }
        return heights.length - 1;
    }
}
```

## 复杂度分析
- 时间：\(O(n\log(\min(n,ladders+1)))\)
- 空间：\(O(\min(n,ladders+1))\)

## 相关题目
- 253 会议室 II：同样用小根堆做贪心决策
