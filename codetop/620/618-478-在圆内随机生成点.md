# 478. 在圆内随机生成点

- 题号：478
- 难度：中等
- LeetCode：[在圆内随机生成点](https://leetcode.cn/problems/generate-random-point-in-a-circle/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 极坐标采样：角度 `theta` 均匀在 \([0, 2\pi)\)；半径要满足面积均匀，所以取 `r * sqrt(U)`（`U` 为 \([0,1)\) 均匀随机数）。

## Java 最佳实现

```java
import java.util.Random;

class Solution {
    private final double radius;
    private final double xCenter;
    private final double yCenter;
    private final Random rand = new Random();

    public Solution(double radius, double x_center, double y_center) {
        this.radius = radius;
        this.xCenter = x_center;
        this.yCenter = y_center;
    }

    public double[] randPoint() {
        double theta = rand.nextDouble() * 2.0 * Math.PI;
        double r = Math.sqrt(rand.nextDouble()) * radius;
        double x = xCenter + r * Math.cos(theta);
        double y = yCenter + r * Math.sin(theta);
        return new double[]{x, y};
    }
}
```

## 复杂度分析
- 时间：\(O(1)\)
- 空间：\(O(1)\)

## 相关题目
- 470 用 Rand7 实现 Rand10：随机分布/拒绝采样
