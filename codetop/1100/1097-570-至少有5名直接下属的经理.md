# 570. 至少有5名直接下属的经理

- 题号：570
- 难度：中等
- LeetCode：[至少有5名直接下属的经理](https://leetcode.cn/problems/managers-with-at-least-5-direct-reports/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- SQL：按 `managerId` 分组统计下属人数，筛出 `count>=5` 的经理 id，再回到 Employee 表取经理名字。

## Java 最佳实现

```java
// 这题提交 SQL
/*
SELECT e.name
FROM Employee e
WHERE e.id IN (
    SELECT managerId
    FROM Employee
    WHERE managerId IS NOT NULL
    GROUP BY managerId
    HAVING COUNT(*) >= 5
);
*/
```

## 复杂度分析
- 时间：取决于数据库执行计划
- 空间：取决于数据库执行计划

## 相关题目
- 185 部门工资前三高的所有员工：SQL 分组/窗口
- 184 部门工资最高的员工：SQL
