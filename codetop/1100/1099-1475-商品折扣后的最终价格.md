# 1475. 商品折扣后的最终价格

- 题号：1475
- 难度：简单
- LeetCode：[商品折扣后的最终价格](https://leetcode.cn/problems/final-prices-with-a-special-discount-in-a-shop/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 单调栈（维护一个递增栈下标）：
  - 从左到右遍历价格
  - 当当前价格 `prices[i]` 小于等于栈顶价格时，说明它是栈顶商品的折扣，弹栈并计算
  - 最后栈里剩下的没有折扣，价格不变

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Deque;

class Solution {
    public int[] finalPrices(int[] prices) {
        int n = prices.length;
        int[] ans = prices.clone();
        Deque<Integer> st = new ArrayDeque<>();
        for (int i = 0; i < n; i++) {
            while (!st.isEmpty() && prices[st.peek()] >= prices[i]) {
                int idx = st.pop();
                ans[idx] = prices[idx] - prices[i];
            }
            st.push(i);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 496 下一个更大元素 I：单调栈
- 739 每日温度：单调栈
