# 888. 公平的糖果交换

- 题号：888
- 难度：简单
- LeetCode：[公平的糖果交换](https://leetcode.cn/problems/fair-candy-swap/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 设 A 总和 `sumA`，B 总和 `sumB`。交换 `a` 和 `b` 后相等：
  - `sumA - a + b = sumB - b + a`
  - 得 `b = a + (sumB - sumA) / 2`
  - 用 HashSet 存 B 的糖果尺寸，枚举 A 找到满足的 `b`

## Java 最佳实现

```java
import java.util.HashSet;
import java.util.Set;

class Solution {
    public int[] fairCandySwap(int[] aliceSizes, int[] bobSizes) {
        int sumA = 0, sumB = 0;
        for (int x : aliceSizes) sumA += x;
        Set<Integer> setB = new HashSet<>();
        for (int x : bobSizes) {
            sumB += x;
            setB.add(x);
        }

        int diff = (sumB - sumA) / 2;
        for (int a : aliceSizes) {
            int b = a + diff;
            if (setB.contains(b)) return new int[]{a, b};
        }
        return new int[0];
    }
}
```

## 复杂度分析
- 时间：\(O(n+m)\)
- 空间：\(O(m)\)

## 相关题目
- 1 两数之和：哈希
- 560 和为K的子数组：哈希
