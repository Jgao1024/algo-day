# 950. 按递增顺序显示卡牌

- 题号：950
- 难度：中等
- LeetCode：[按递增顺序显示卡牌](https://leetcode.cn/problems/reveal-cards-in-increasing-order/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 反向模拟：
  - 最终展示顺序是递增的，因此把牌从大到小“反推”初始牌堆
  - 用双端队列 `dq` 存当前牌堆（队首为顶）
    - 每次先把队尾元素移到队首（对应正向的“把顶牌移到底”）
    - 再把当前最大牌放到队首

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Arrays;
import java.util.Deque;

class Solution {
    public int[] deckRevealedIncreasing(int[] deck) {
        Arrays.sort(deck);
        Deque<Integer> dq = new ArrayDeque<>();
        for (int i = deck.length - 1; i >= 0; i--) {
            if (!dq.isEmpty()) dq.addFirst(dq.removeLast());
            dq.addFirst(deck[i]);
        }
        int[] ans = new int[deck.length];
        int i = 0;
        while (!dq.isEmpty()) ans[i++] = dq.removeFirst();
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n\log n)\)
- 空间：\(O(n)\)

## 相关题目
- 239 滑动窗口最大值：双端队列
- 950 本题：队列模拟
