# 219. 存在重复元素 II

- 题号：219
- 难度：简单
- LeetCode：[存在重复元素 II](https://leetcode.cn/problems/contains-duplicate-ii/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 用 HashMap 记录每个数最近一次出现的位置：
  - 若当前下标 `i - last[val] <= k`，返回 true
  - 更新 `last[val] = i`

## Java 最佳实现

```java
import java.util.HashMap;
import java.util.Map;

class Solution {
    public boolean containsNearbyDuplicate(int[] nums, int k) {
        Map<Integer, Integer> last = new HashMap<>();
        for (int i = 0; i < nums.length; i++) {
            Integer pre = last.put(nums[i], i);
            if (pre != null && i - pre <= k) return true;
        }
        return false;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 217 存在重复元素：集合去重
- 219 本题：最近下标
