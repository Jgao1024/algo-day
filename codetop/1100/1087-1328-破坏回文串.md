# 1328. 破坏回文串

- 题号：1328
- 难度：中等
- LeetCode：[破坏回文串](https://leetcode.cn/problems/break-a-palindrome/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 贪心：
  - 若长度为 1，无法破坏返回空串
  - 在前半段找到第一个不是 `'a'` 的字符，改成 `'a'`（字典序最小且破坏回文）
  - 若前半段全是 `'a'`，把最后一个字符改成 `'b'`

## Java 最佳实现

```java
class Solution {
    public String breakPalindrome(String palindrome) {
        int n = palindrome.length();
        if (n == 1) return "";
        char[] a = palindrome.toCharArray();
        for (int i = 0; i < n / 2; i++) {
            if (a[i] != 'a') {
                a[i] = 'a';
                return new String(a);
            }
        }
        a[n - 1] = 'b';
        return new String(a);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 680 验证回文串 II：回文
- 5 最长回文子串：回文
