# 143. 重排链表

- 题号：143
- 难度：中等
- LeetCode：[重排链表](https://leetcode.cn/problems/reorder-list/)

## 题目描述
给定一个单链表 `L0→L1→…→Ln-1→Ln`，将其重排为：`L0→Ln→L1→Ln-1→L2→Ln-2→…`。

要求原地修改链表节点（不能只是修改值）。

## 示例
- 示例  
输入：`1->2->3->4`  
输出：`1->4->2->3`

## 最佳解法思路
- 思路1（推荐）：找中点 + 反转后半段 + 交错合并  
1) 快慢指针找中点，把链表断成两半  
2) 反转后半段  
3) 两段交错合并：`first.next=second; second.next=firstNext`

## Java 最佳实现

```java
class Solution {
    public void reorderList(ListNode head) {
        if (head == null || head.next == null) return;

        ListNode slow = head, fast = head;
        while (fast.next != null && fast.next.next != null) {
            slow = slow.next;
            fast = fast.next.next;
        }

        ListNode second = slow.next;
        slow.next = null;
        second = reverse(second);

        ListNode first = head;
        while (second != null) {
            ListNode t1 = first.next;
            ListNode t2 = second.next;
            first.next = second;
            second.next = t1;
            first = t1;
            second = t2;
        }
    }

    private ListNode reverse(ListNode head) {
        ListNode prev = null, cur = head;
        while (cur != null) {
            ListNode next = cur.next;
            cur.next = prev;
            prev = cur;
            cur = next;
        }
        return prev;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 206 反转链表：反转后半段用到
- 876 链表的中间结点：找中点
- 234 回文链表：中点 + 反转后半段
- 143/234：一个交错合并，一个对比判断

## 总结（速记）
- 组合拳：**中点 + 反转 + 合并**，链表题高频套路。
