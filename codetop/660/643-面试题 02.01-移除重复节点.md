# 面试题 02.01. 移除重复节点

- 题号：面试题 02.01
- 难度：简单
- LeetCode：[移除重复节点](https://leetcode.cn/problems/remove-duplicate-node-lcci/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 用 HashSet 记录已经出现过的值，遍历链表：
  - 若当前值已出现，跳过该节点
  - 否则加入集合并继续

## Java 最佳实现

```java
import java.util.HashSet;
import java.util.Set;

/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode(int x) { val = x; }
 * }
 */
class Solution {
    public ListNode removeDuplicateNodes(ListNode head) {
        if (head == null) return null;
        Set<Integer> set = new HashSet<>();
        set.add(head.val);

        ListNode cur = head;
        while (cur.next != null) {
            if (set.contains(cur.next.val)) {
                cur.next = cur.next.next;
            } else {
                set.add(cur.next.val);
                cur = cur.next;
            }
        }
        return head;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 83 删除排序链表中的重复元素：有序链表去重
- 82 删除排序链表中的重复元素 II：删除所有重复值
