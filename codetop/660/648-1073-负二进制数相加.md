# 1073. 负二进制数相加

- 题号：1073
- 难度：中等
- LeetCode：[负二进制数相加](https://leetcode.cn/problems/adding-two-negabinary-numbers/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 按位相加 + 进位（基数为 -2）：
  - 从低位到高位累加 `sum = a + b + carry`
  - 当前位 `digit = sum & 1`（保证是 0/1）
  - 新进位 `carry = (sum - digit) / -2`（`sum-digit` 一定是偶数）
  - 去掉结果最高位多余的 0

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public int[] addNegabinary(int[] arr1, int[] arr2) {
        int i = arr1.length - 1;
        int j = arr2.length - 1;
        int carry = 0;

        List<Integer> res = new ArrayList<>();
        while (i >= 0 || j >= 0 || carry != 0) {
            int sum = carry;
            if (i >= 0) sum += arr1[i--];
            if (j >= 0) sum += arr2[j--];

            int digit = sum & 1;
            res.add(digit);
            carry = (sum - digit) / -2;
        }

        while (res.size() > 1 && res.get(res.size() - 1) == 0) {
            res.remove(res.size() - 1);
        }

        int[] ans = new int[res.size()];
        for (int k = 0; k < res.size(); k++) {
            ans[k] = res.get(res.size() - 1 - k);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n+m)\)
- 空间：\(O(n+m)\)

## 相关题目
- 2 两数相加：进位加法
- 67 二进制求和：正二进制加法
