# 376. 摆动序列

- 题号：376
- 难度：中等
- LeetCode：[摆动序列](https://leetcode.cn/problems/wiggle-subsequence/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 贪心 / DP（推荐）
- `up`：以“上升”结尾的最长摆动长度；`down`：以“下降”结尾的最长摆动长度。
- 遍历相邻差：
  - `nums[i] > nums[i-1]`：`up = down + 1`
  - `nums[i] < nums[i-1]`：`down = up + 1`

## Java 最佳实现

```java
class Solution {
    public int wiggleMaxLength(int[] nums) {
        if (nums.length == 0) return 0;
        int up = 1, down = 1;
        for (int i = 1; i < nums.length; i++) {
            if (nums[i] > nums[i - 1]) {
                up = down + 1;
            } else if (nums[i] < nums[i - 1]) {
                down = up + 1;
            }
        }
        return Math.max(up, down);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 300 最长递增子序列
