# 382. 链表随机节点

- 题号：382
- 难度：中等
- LeetCode：[链表随机节点](https://leetcode.cn/problems/linked-list-random-node/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 蓄水池抽样（推荐）
- 只遍历一次链表，在不知道长度的情况下保证每个节点被选中的概率相同。
- 第 `i` 个节点以 `1/i` 的概率替换当前答案。

## Java 最佳实现

```java
import java.util.Random;

class Solution {
    private final ListNode head;
    private final Random random = new Random();

    public Solution(ListNode head) {
        this.head = head;
    }

    public int getRandom() {
        int ans = 0;
        int i = 0;
        for (ListNode cur = head; cur != null; cur = cur.next) {
            i++;
            if (random.nextInt(i) == 0) {
                ans = cur.val;
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：每次 `getRandom` 为 \(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 398 随机数索引（相同思想）
