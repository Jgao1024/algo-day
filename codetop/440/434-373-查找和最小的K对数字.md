# 373. 查找和最小的K对数字

- 题号：373
- 难度：中等
- LeetCode：[查找和最小的K对数字](https://leetcode.cn/problems/find-k-pairs-with-smallest-sums/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 小根堆（推荐）
- `nums1[i]` 固定时，与 `nums2` 组成的和随 `j` 递增。把每个 i 的 `(i,0)` 入堆。
- 每次弹出最小 `(i,j)`，加入答案，并把 `(i,j+1)` 入堆（若存在），重复 k 次。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;
import java.util.PriorityQueue;

class Solution {
    public List<List<Integer>> kSmallestPairs(int[] nums1, int[] nums2, int k) {
        List<List<Integer>> ans = new ArrayList<>();
        if (nums1.length == 0 || nums2.length == 0 || k == 0) return ans;

        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[0] - b[0]);
        int n1 = nums1.length, n2 = nums2.length;
        int limit = Math.min(k, n1);
        for (int i = 0; i < limit; i++) {
            pq.offer(new int[]{nums1[i] + nums2[0], i, 0});
        }

        while (k > 0 && !pq.isEmpty()) {
            int[] cur = pq.poll();
            int i = cur[1], j = cur[2];
            List<Integer> pair = new ArrayList<>(2);
            pair.add(nums1[i]);
            pair.add(nums2[j]);
            ans.add(pair);
            k--;

            if (j + 1 < n2) {
                pq.offer(new int[]{nums1[i] + nums2[j + 1], i, j + 1});
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(k \log \min(k, n_1))\)
- 空间：\(O(\min(k, n_1))\)

## 相关题目
- 378 有序矩阵中第 K 小的元素（堆 / 二分）
