# 448. 找到所有数组中消失的数字

- 题号：448
- 难度：简单
- LeetCode：[找到所有数组中消失的数字](https://leetcode.cn/problems/find-all-numbers-disappeared-in-an-array/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 原地标记（推荐）
- 数组元素在 `[1,n]`。遍历每个数 `x`，把下标 `abs(x)-1` 位置标记为负数。
- 最后仍为正数的位置 `i` 表示数字 `i+1` 没出现。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<Integer> findDisappearedNumbers(int[] nums) {
        for (int i = 0; i < nums.length; i++) {
            int idx = Math.abs(nums[i]) - 1;
            if (nums[idx] > 0) nums[idx] = -nums[idx];
        }

        List<Integer> ans = new ArrayList<>();
        for (int i = 0; i < nums.length; i++) {
            if (nums[i] > 0) ans.add(i + 1);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)（不计返回集合）

## 相关题目
- 442 数组中重复的数据
