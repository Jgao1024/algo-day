# 补充题10. 36进制减法

- 题号：补充题10
- 难度：中等
- LeetCode：[36进制减法](https://leetcode.cn/problems/https://mp.weixin.qq.com/s/ub9GpTBjDF55hZld3V2rEA/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 竖式减法（推荐）
- 从低位到高位逐位相减，处理借位；字符映射 `0-9a-z` 到 0~35。
- 去掉前导 0（结果为 0 时保留一个 0）。

## Java 最佳实现

```java
class Solution {
    public String sub36(String a, String b) {
        int i = a.length() - 1;
        int j = b.length() - 1;
        int borrow = 0;
        StringBuilder sb = new StringBuilder();

        while (i >= 0 || j >= 0) {
            int x = i >= 0 ? val(a.charAt(i--)) : 0;
            int y = j >= 0 ? val(b.charAt(j--)) : 0;
            int t = x - borrow - y;
            if (t < 0) {
                t += 36;
                borrow = 1;
            } else {
                borrow = 0;
            }
            sb.append(digit(t));
        }

        while (sb.length() > 1 && sb.charAt(sb.length() - 1) == '0') {
            sb.setLength(sb.length() - 1);
        }
        return sb.reverse().toString();
    }

    private int val(char c) {
        if (c >= '0' && c <= '9') return c - '0';
        c = Character.toLowerCase(c);
        return c - 'a' + 10;
    }

    private char digit(int v) {
        if (v < 10) return (char) ('0' + v);
        return (char) ('a' + (v - 10));
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 415 字符串相加（同类竖式）
