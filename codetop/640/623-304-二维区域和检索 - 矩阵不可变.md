# 304. 二维区域和检索 - 矩阵不可变

- 题号：304
- 难度：中等
- LeetCode：[二维区域和检索 - 矩阵不可变](https://leetcode.cn/problems/range-sum-query-2d-immutable/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 二维前缀和（推荐）
- 预处理 `pre[i+1][j+1]` 表示 `(0,0)` 到 `(i,j)` 的矩形和。
- 查询子矩形用容斥：
  - `sum = pre[r2+1][c2+1] - pre[r1][c2+1] - pre[r2+1][c1] + pre[r1][c1]`

## Java 最佳实现

```java
class NumMatrix {
    private final int[][] pre;

    public NumMatrix(int[][] matrix) {
        int m = matrix.length;
        int n = m == 0 ? 0 : matrix[0].length;
        pre = new int[m + 1][n + 1];
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                pre[i + 1][j + 1] = pre[i + 1][j] + pre[i][j + 1] - pre[i][j] + matrix[i][j];
            }
        }
    }

    public int sumRegion(int row1, int col1, int row2, int col2) {
        return pre[row2 + 1][col2 + 1] - pre[row1][col2 + 1] - pre[row2 + 1][col1] + pre[row1][col1];
    }
}
```

## 复杂度分析
- 预处理时间：\(O(mn)\)，单次查询：\(O(1)\)
- 空间：\(O(mn)\)

## 相关题目
- 303 区域和检索 - 数组不可变
