# 842. 将数组拆分成斐波那契序列

- 题号：842
- 难度：中等
- LeetCode：[将数组拆分成斐波那契序列](https://leetcode.cn/problems/split-array-into-fibonacci-sequence/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 回溯（推荐）
- 从左到右切分数字，每段转成整数（不能有前导 0，且不超过 32 位 int）。
- 当已选的序列长度 >= 2 时，下一项必须等于前两项之和；若当前值大于目标和可直接剪枝。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<Integer> splitIntoFibonacci(String num) {
        List<Integer> ans = new ArrayList<>();
        dfs(num, 0, ans);
        return ans;
    }

    private boolean dfs(String s, int idx, List<Integer> seq) {
        if (idx == s.length()) return seq.size() >= 3;

        long cur = 0;
        for (int i = idx; i < s.length(); i++) {
            if (i > idx && s.charAt(idx) == '0') break;
            cur = cur * 10 + (s.charAt(i) - '0');
            if (cur > Integer.MAX_VALUE) break;

            int size = seq.size();
            if (size >= 2) {
                long sum = (long) seq.get(size - 1) + seq.get(size - 2);
                if (cur < sum) continue;
                if (cur > sum) break;
            }

            seq.add((int) cur);
            if (dfs(s, i + 1, seq)) return true;
            seq.remove(seq.size() - 1);
        }
        return false;
    }
}
```

## 复杂度分析
- 时间：搜索 + 剪枝（最坏指数级）
- 空间：\(O(n)\)（递归栈与路径）

## 相关题目
- 306 累加数
