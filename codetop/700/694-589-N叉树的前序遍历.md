# 589. N叉树的前序遍历

- 题号：589
- 难度：简单
- LeetCode：[N叉树的前序遍历](https://leetcode.cn/problems/n-ary-tree-preorder-traversal/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：迭代栈（或递归）  
用栈模拟前序：先压根，弹出访问后，把子节点从右到左压栈即可保证从左到右访问。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.ArrayDeque;
import java.util.Deque;
import java.util.List;

class Solution {
    public List<Integer> preorder(Node root) {
        List<Integer> ans = new ArrayList<>();
        if (root == null) {
            return ans;
        }
        Deque<Node> stack = new ArrayDeque<>();
        stack.push(root);
        while (!stack.isEmpty()) {
            Node node = stack.pop();
            ans.add(node.val);
            if (node.children == null) {
                continue;
            }
            for (int i = node.children.size() - 1; i >= 0; i--) {
                Node child = node.children.get(i);
                if (child != null) {
                    stack.push(child);
                }
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)

## 相关题目
- 590 N 叉树的后序遍历
