# 901. 股票价格跨度

- 题号：901
- 难度：中等
- LeetCode：[股票价格跨度](https://leetcode.cn/problems/online-stock-span/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：单调栈（存 price + span）  
对每个新价格，把栈顶所有 `price <= 当前price` 的元素弹出并累加它们的 `span`，最后把 `(price, span)` 入栈，`span` 即答案。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Deque;

class StockSpanner {
    private final Deque<int[]> stack = new ArrayDeque<>();

    public int next(int price) {
        int span = 1;
        while (!stack.isEmpty() && stack.peek()[0] <= price) {
            span += stack.pop()[1];
        }
        stack.push(new int[]{price, span});
        return span;
    }
}
```

## 复杂度分析
- 时间：均摊 \(O(1)\)
- 空间：\(O(n)\)

## 相关题目
- 739 每日温度：单调栈
