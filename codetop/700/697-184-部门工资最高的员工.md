# 184. 部门工资最高的员工

- 题号：184
- 难度：中等
- LeetCode：[部门工资最高的员工](https://leetcode.cn/problems/department-highest-salary/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路：分组取最大值再关联部门名  
先算每个部门最高薪资，再与员工表、部门表 join 取出对应员工。

## SQL 最佳实现

```sql
SELECT d.Name AS Department, e.Name AS Employee, e.Salary AS Salary
FROM Employee e
JOIN Department d ON e.DepartmentId = d.Id
JOIN (
    SELECT DepartmentId, MAX(Salary) AS maxSalary
    FROM Employee
    GROUP BY DepartmentId
) t ON t.DepartmentId = e.DepartmentId AND t.maxSalary = e.Salary;
```

## 复杂度分析
- 时间：取决于索引与执行计划
- 空间：取决于执行计划

## 相关题目
- 176 第二高的薪水
