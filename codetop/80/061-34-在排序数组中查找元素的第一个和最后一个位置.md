# 34. 在排序数组中查找元素的第一个和最后一个位置

- 题号：34
- 难度：中等
- LeetCode：[在排序数组中查找元素的第一个和最后一个位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：二分找边界  
分别找：
  - 第一个 \(\ge target\) 的位置（left）
  - 第一个 \(> target\) 的位置（right + 1）
如果 left 越界或 nums[left] != target，则不存在。

## Java 最佳实现

```java
class Solution {
    public int[] searchRange(int[] nums, int target) {
        int left = lowerBound(nums, target);
        if (left == nums.length || nums[left] != target) return new int[]{-1, -1};
        int right = upperBound(nums, target) - 1;
        return new int[]{left, right};
    }

    private int lowerBound(int[] nums, int target) {
        int l = 0, r = nums.length; // [l, r)
        while (l < r) {
            int m = l + (r - l) / 2;
            if (nums[m] >= target) r = m;
            else l = m + 1;
        }
        return l;
    }

    private int upperBound(int[] nums, int target) {
        int l = 0, r = nums.length; // [l, r)
        while (l < r) {
            int m = l + (r - l) / 2;
            if (nums[m] > target) r = m;
            else l = m + 1;
        }
        return l;
    }
}
```

## 复杂度分析
- 时间：\(O(\log n)\)
- 空间：\(O(1)\)

## 相关题目
- 704 二分查找
- 35 搜索插入位置
- 33 搜索旋转排序数组

## 总结（速记）
- 左边界：找第一个 \(\ge target\)
- 右边界：找第一个 \(> target\) 再减 1
