# 39. 组合总和

- 题号：39
- 难度：中等
- LeetCode：[组合总和](https://leetcode.cn/problems/combination-sum/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路（推荐）：回溯 + 剪枝  
每次选择一个数加入路径，剩余目标值减少；同一个数可以重复选，所以递归时 `start` 不变。  
候选数组先排序，若当前数大于剩余目标可直接停止本层循环。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class Solution {
    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        Arrays.sort(candidates);
        List<List<Integer>> ans = new ArrayList<>();
        dfs(candidates, target, 0, new ArrayList<>(), ans);
        return ans;
    }

    private void dfs(int[] candidates, int remain, int start, List<Integer> path, List<List<Integer>> ans) {
        if (remain == 0) {
            ans.add(new ArrayList<>(path));
            return;
        }
        for (int i = start; i < candidates.length; i++) {
            int x = candidates[i];
            if (x > remain) break;
            path.add(x);
            dfs(candidates, remain - x, i, path, ans);
            path.remove(path.size() - 1);
        }
    }
}
```

## 复杂度分析
- 时间：指数级（取决于解的数量与搜索树大小）
- 空间：\(O(target)\)（递归深度/路径长度级别，忽略答案占用）

## 相关题目
- 40 组合总和 II（元素只能用一次）
- 216 组合总和 III

## 总结（速记）
- 允许重复选：递归时下标不前进（`dfs(..., i, ...)`）。
