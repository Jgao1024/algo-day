# 1200. 最小绝对差

- 题号：1200
- 难度：简单
- LeetCode：[最小绝对差](https://leetcode.cn/problems/minimum-absolute-difference/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **排序后看相邻差**  
最小绝对差一定出现在排序后相邻元素之间。先排序，扫描求最小差，再二次扫描收集所有满足最小差的相邻对。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class Solution {
    public List<List<Integer>> minimumAbsDifference(int[] arr) {
        Arrays.sort(arr);
        int best = Integer.MAX_VALUE;
        for (int i = 1; i < arr.length; i++) {
            best = Math.min(best, arr[i] - arr[i - 1]);
        }

        List<List<Integer>> ans = new ArrayList<>();
        for (int i = 1; i < arr.length; i++) {
            if (arr[i] - arr[i - 1] == best) {
                List<Integer> p = new ArrayList<>(2);
                p.add(arr[i - 1]);
                p.add(arr[i]);
                ans.add(p);
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n \log n)\)
- 空间：\(O(1)\)（排序额外空间视实现）

## 相关题目
- 16 最接近的三数之和：排序 + 扫描
