# 286. 墙与门

- 题号：286
- 难度：中等
- LeetCode：[墙与门](https://leetcode.cn/problems/walls-and-gates/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **多源 BFS**  
把所有门（值为 0）同时入队，从这些点往外扩散；每次走到一个房间（INF）就更新为当前距离 +1 并入队。  
这样每个房间第一次被更新时就是到最近门的最短距离。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Queue;

class Solution {
    private static final int INF = 2147483647;
    private static final int[] DR = {1, -1, 0, 0};
    private static final int[] DC = {0, 0, 1, -1};

    public void wallsAndGates(int[][] rooms) {
        int m = rooms.length;
        int n = rooms[0].length;
        Queue<int[]> q = new ArrayDeque<>();
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (rooms[i][j] == 0) q.add(new int[]{i, j});
            }
        }

        while (!q.isEmpty()) {
            int[] cur = q.poll();
            int r = cur[0];
            int c = cur[1];
            for (int d = 0; d < 4; d++) {
                int nr = r + DR[d];
                int nc = c + DC[d];
                if (nr < 0 || nr >= m || nc < 0 || nc >= n) continue;
                if (rooms[nr][nc] != INF) continue;
                rooms[nr][nc] = rooms[r][c] + 1;
                q.add(new int[]{nr, nc});
            }
        }
    }
}
```

## 复杂度分析
- 时间：\(O(mn)\)
- 空间：\(O(mn)\)

## 相关题目
- 542 01 矩阵：同类多源 BFS
