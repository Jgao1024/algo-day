# 1431. 拥有最多糖果的孩子

- 题号：1431
- 难度：简单
- LeetCode：[拥有最多糖果的孩子](https://leetcode.cn/problems/kids-with-the-greatest-number-of-candies/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **先求最大值再逐个判断**  
先求当前最大糖果数 `max`，对每个孩子判断 `candies[i] + extraCandies >= max`。

## Java 最佳实现

```java
import java.util.ArrayList;
import java.util.List;

class Solution {
    public List<Boolean> kidsWithCandies(int[] candies, int extraCandies) {
        int max = 0;
        for (int x : candies) if (x > max) max = x;
        List<Boolean> ans = new ArrayList<>(candies.length);
        for (int x : candies) {
            ans.add(x + extraCandies >= max);
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)（不计返回结果）

## 相关题目
- 1732 找到最高海拔：一次扫描
