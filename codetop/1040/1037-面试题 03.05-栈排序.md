# 面试题 03.05. 栈排序

- 题号：面试题 03.05
- 难度：中等
- LeetCode：[栈排序](https://leetcode.cn/problems/sort-of-stacks-lcci/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 用一个辅助栈做插入排序，始终保持主栈“栈顶是最小值”。
- push 时把更小的元素暂存到辅助栈，插入后再放回。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Deque;

class SortedStack {
    private final Deque<Integer> stack = new ArrayDeque<>();
    private final Deque<Integer> help = new ArrayDeque<>();

    public SortedStack() {
    }

    public void push(int val) {
        while (!stack.isEmpty() && stack.peek() < val) {
            help.push(stack.pop());
        }
        stack.push(val);
        while (!help.isEmpty()) {
            stack.push(help.pop());
        }
    }

    public void pop() {
        if (!stack.isEmpty()) stack.pop();
    }

    public int peek() {
        return stack.isEmpty() ? -1 : stack.peek();
    }

    public boolean isEmpty() {
        return stack.isEmpty();
    }
}
```

## 复杂度分析
- 时间：push 最坏 \(O(n)\)，pop/peek \(O(1)\)
- 空间：\(O(n)\)

## 相关题目
- 155 最小栈：栈结构题
