# 886. 可能的二分法

- 题号：886
- 难度：中等
- LeetCode：[可能的二分法](https://leetcode.cn/problems/possible-bipartition/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 二分图染色（推荐）  
把 `dislikes` 看作无向图；如果图可二分，则可把人分成两组。  
用 BFS/DFS 给点染色（1/-1），遇到相邻同色就失败。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Deque;
import java.util.List;

class Solution {
    public boolean possibleBipartition(int n, int[][] dislikes) {
        List<List<Integer>> g = new ArrayList<>(n + 1);
        for (int i = 0; i <= n; i++) g.add(new ArrayList<>());
        for (int[] e : dislikes) {
            g.get(e[0]).add(e[1]);
            g.get(e[1]).add(e[0]);
        }

        int[] color = new int[n + 1];
        Deque<Integer> q = new ArrayDeque<>();

        for (int i = 1; i <= n; i++) {
            if (color[i] != 0) continue;
            color[i] = 1;
            q.add(i);
            while (!q.isEmpty()) {
                int u = q.pollFirst();
                for (int v : g.get(u)) {
                    if (color[v] == 0) {
                        color[v] = -color[u];
                        q.add(v);
                    } else if (color[v] == color[u]) {
                        return false;
                    }
                }
            }
        }
        return true;
    }
}
```

## 复杂度分析
- 时间：\(O(n+m)\)
- 空间：\(O(n+m)\)

## 相关题目
- 785 判断二分图

## 总结（速记）
- **“可能二分”= 判断图是否二分图（染色冲突就 false）**。
