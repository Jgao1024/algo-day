# 65. 有效数字

- 题号：65
- 难度：困难
- LeetCode：[有效数字](https://leetcode.cn/problems/valid-number/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 扫描 + 状态标记（推荐）  
依次扫描字符，维护：  
- `seenDigit`：是否出现过数字  
- `seenDot`：是否出现过小数点  
- `seenExp`：是否出现过 `e/E`  
- `digitAfterExp`：`e/E` 后是否出现过数字  
规则：  
- `+/-` 只能出现在开头或 `e/E` 后  
- `.` 只能出现一次且不能在 `e/E` 后  
- `e/E` 只能出现一次且前面必须有数字  
- 结束时必须 `seenDigit && digitAfterExp`

## Java 最佳实现

```java
class Solution {
    public boolean isNumber(String s) {
        if (s == null) return false;
        s = s.trim();
        if (s.length() == 0) return false;

        boolean seenDigit = false;
        boolean seenDot = false;
        boolean seenExp = false;
        boolean digitAfterExp = true;

        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            if (c >= '0' && c <= '9') {
                seenDigit = true;
                if (seenExp) digitAfterExp = true;
            } else if (c == '+' || c == '-') {
                if (i != 0) {
                    char prev = s.charAt(i - 1);
                    if (prev != 'e' && prev != 'E') return false;
                }
            } else if (c == '.') {
                if (seenDot || seenExp) return false;
                seenDot = true;
            } else if (c == 'e' || c == 'E') {
                if (seenExp || !seenDigit) return false;
                seenExp = true;
                digitAfterExp = false;
            } else {
                return false;
            }
        }
        return seenDigit && digitAfterExp;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 8 字符串转换整数 (atoi)

## 总结（速记）
- **`+/-` 位置受限；`.` 不能在指数后；`e/E` 前后都要“有数字”**。
