# 951. 翻转等价二叉树

- 题号：951
- 难度：中等
- LeetCode：[翻转等价二叉树](https://leetcode.cn/problems/flip-equivalent-binary-trees/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 递归判断（推荐）  
两棵树翻转等价：  
根值相等，且满足下面二选一：  
- 不翻：`left` 对 `left` 且 `right` 对 `right`  
- 翻转：`left` 对 `right` 且 `right` 对 `left`

## Java 最佳实现

```java
class Solution {
    public boolean flipEquiv(TreeNode root1, TreeNode root2) {
        if (root1 == null && root2 == null) return true;
        if (root1 == null || root2 == null) return false;
        if (root1.val != root2.val) return false;

        boolean same = flipEquiv(root1.left, root2.left) && flipEquiv(root1.right, root2.right);
        if (same) return true;
        return flipEquiv(root1.left, root2.right) && flipEquiv(root1.right, root2.left);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)（每个节点最多比较常数次）
- 空间：\(O(h)\)（递归栈）

## 相关题目
- 100 相同的树

## 总结（速记）
- **每层做一次“同向/交叉”两种匹配**。
