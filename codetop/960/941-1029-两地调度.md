# 1029. 两地调度

- 题号：1029
- 难度：简单
- LeetCode：[两地调度](https://leetcode.cn/problems/two-city-scheduling/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 贪心：每个人去 A 或 B，关键是“去 A 比去 B 多花/少花多少钱”。
  - 计算差值 `diff = costA - costB`，按 diff 升序排序
  - 前一半人去 A，后一半人去 B

## Java 最佳实现

```java
import java.util.Arrays;

class Solution {
    public int twoCitySchedCost(int[][] costs) {
        Arrays.sort(costs, (a, b) -> (a[0] - a[1]) - (b[0] - b[1]));
        int n = costs.length / 2;
        int ans = 0;
        for (int i = 0; i < costs.length; i++) {
            ans += (i < n) ? costs[i][0] : costs[i][1];
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n\log n)\)
- 空间：\(O(1)\)（不算排序）

## 相关题目
- 455 分发饼干：贪心
- 406 根据身高重建队列：排序 + 贪心
