# 1212. 查询球队积分

- 题号：1212
- 难度：中等
- LeetCode：[查询球队积分](https://leetcode.cn/problems/team-scores-in-football-tournament/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- SQL：把每场比赛拆成两条记录（主队/客队），按胜/平/负累积分数，再左连接到 Teams 确保无比赛的队也输出 0 分，按分数降序、team_id 升序。

## Java 最佳实现

```java
// 这题提交 SQL
/*
SELECT t.team_id,
       t.team_name,
       COALESCE(SUM(s.points), 0) AS num_points
FROM Teams t
LEFT JOIN (
    SELECT host_team AS team_id,
           CASE
               WHEN host_goals > guest_goals THEN 3
               WHEN host_goals = guest_goals THEN 1
               ELSE 0
           END AS points
    FROM Matches
    UNION ALL
    SELECT guest_team AS team_id,
           CASE
               WHEN guest_goals > host_goals THEN 3
               WHEN guest_goals = host_goals THEN 1
               ELSE 0
           END AS points
    FROM Matches
) s ON s.team_id = t.team_id
GROUP BY t.team_id, t.team_name
ORDER BY num_points DESC, t.team_id ASC;
*/
```

## 复杂度分析
- 时间：取决于数据库执行计划
- 空间：取决于数据库执行计划

## 相关题目
- 184 部门工资最高的员工：SQL
- 185 部门工资前三高的所有员工：SQL
