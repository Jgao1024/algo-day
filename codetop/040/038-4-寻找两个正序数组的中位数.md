# 4. 寻找两个正序数组的中位数

- 题号：4
- 难度：困难
- LeetCode：[寻找两个正序数组的中位数](https://leetcode.cn/problems/median-of-two-sorted-arrays/)

## 题目描述
给定两个大小分别为 `m` 和 `n` 的正序数组 `nums1`、`nums2`，返回这两个正序数组的中位数。

要求时间复杂度 \(O(\log(m+n))\)。

## 示例
- 示例1  
输入：`nums1 = [1,3], nums2 = [2]`  
输出：`2.0`

- 示例2  
输入：`nums1 = [1,2], nums2 = [3,4]`  
输出：`2.5`

## 最佳解法思路
- 思路1（推荐）：二分切分（partition）  
在较短数组上二分一个切分点 `i`，另一个数组切分点 `j` 由总长度决定。  
满足条件：`A[i-1] <= B[j]` 且 `B[j-1] <= A[i]`，则切分正确。  
中位数由左半最大与右半最小决定（奇偶分别处理）。

- 思路2：找第 k 小（递归/迭代）  
每次丢掉 k/2 个元素，复杂度同样 \(O(\log(m+n))\)，代码更长。

## Java 最佳实现

```java
class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        if (nums1.length > nums2.length) return findMedianSortedArrays(nums2, nums1);
        int m = nums1.length;
        int n = nums2.length;

        int totalLeft = (m + n + 1) / 2;
        int l = 0, r = m;

        while (l <= r) {
            int i = (l + r) >>> 1;
            int j = totalLeft - i;

            int aLeft = (i == 0) ? Integer.MIN_VALUE : nums1[i - 1];
            int aRight = (i == m) ? Integer.MAX_VALUE : nums1[i];
            int bLeft = (j == 0) ? Integer.MIN_VALUE : nums2[j - 1];
            int bRight = (j == n) ? Integer.MAX_VALUE : nums2[j];

            if (aLeft <= bRight && bLeft <= aRight) {
                if (((m + n) & 1) == 1) {
                    return Math.max(aLeft, bLeft);
                }
                int leftMax = Math.max(aLeft, bLeft);
                int rightMin = Math.min(aRight, bRight);
                return (leftMax + rightMin) / 2.0;
            }

            if (aLeft > bRight) r = i - 1;
            else l = i + 1;
        }
        return 0.0;
    }
}
```

## 复杂度分析
- 时间：\(O(\log \min(m,n))\)
- 空间：\(O(1)\)

## 相关题目
- 215 第K大元素：选择问题
- 4/215：一个是有序数组中位数（切分），一个是无序数组选择（partition）
- 33 旋转数组查找：二分思维

## 总结（速记）
- 这题核心是“把两数组切成左半/右半”，保证左半都 <= 右半，然后从边界取答案。
