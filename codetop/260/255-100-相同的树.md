# 100. 相同的树

- 题号：100
- 难度：简单
- LeetCode：[相同的树](https://leetcode.cn/problems/same-tree/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 思路1（推荐）：递归同时遍历  
两棵树相同需要：  
- 结构相同  
- 对应节点值相同  
递归判断左右子树即可。

## Java 最佳实现

```java
class Solution {
    public boolean isSameTree(TreeNode p, TreeNode q) {
        if (p == null && q == null) return true;
        if (p == null || q == null) return false;
        if (p.val != q.val) return false;
        return isSameTree(p.left, q.left) && isSameTree(p.right, q.right);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)（递归栈）

## 相关题目
- 101 对称二叉树
- 1008 前序遍历构造二叉搜索树

## 总结（速记）
- 两树比较：先判空，再判值，再递归左右。  
