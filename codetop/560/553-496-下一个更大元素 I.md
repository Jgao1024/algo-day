# 496. 下一个更大元素 I

- 题号：496
- 难度：简单
- LeetCode：[下一个更大元素 I](https://leetcode.cn/problems/next-greater-element-i/)

## 题目描述
给定数组 `nums1` 和 `nums2`（`nums1` 是 `nums2` 的子集，且元素唯一），对 `nums1` 中每个元素，找它在 `nums2` 中右侧第一个更大的元素；不存在则为 `-1`。

## 示例
- 示例  
输入：`nums1 = [4,1,2], nums2 = [1,3,4,2]`  
输出：`[-1,3,-1]`

## 最佳解法思路
- 单调栈 + 哈希表（推荐）  
在 `nums2` 上用单调递减栈求每个元素的下一个更大值，并写入 `map[value]=nextGreater`。  
最后按 `nums1` 查表输出。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.Deque;
import java.util.HashMap;
import java.util.Map;

class Solution {
    public int[] nextGreaterElement(int[] nums1, int[] nums2) {
        Map<Integer, Integer> next = new HashMap<>();
        Deque<Integer> st = new ArrayDeque<>();
        for (int x : nums2) {
            while (!st.isEmpty() && st.peek() < x) {
                next.put(st.pop(), x);
            }
            st.push(x);
        }
        while (!st.isEmpty()) next.put(st.pop(), -1);

        int[] ans = new int[nums1.length];
        for (int i = 0; i < nums1.length; i++) ans[i] = next.get(nums1[i]);
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(|nums1|+|nums2|)\)
- 空间：\(O(|nums2|)\)

## 相关题目
- 739 每日温度
- 503 下一个更大元素 II

## 总结（速记）
- **先在 nums2 用单调栈算 nextGreater，再对 nums1 查表**。