# 81. 搜索旋转排序数组 II

- 题号：81
- 难度：中等
- LeetCode：[搜索旋转排序数组 II](https://leetcode.cn/problems/search-in-rotated-sorted-array-ii/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 二分：与 33 类似，但有重复元素时可能无法判断哪边有序。
- 当 `nums[l] == nums[mid] == nums[r]` 时，收缩边界 `l++`、`r--` 去掉干扰；否则按“哪边有序”判断并缩小区间。

## Java 最佳实现

```java
class Solution {
    public boolean search(int[] nums, int target) {
        int l = 0, r = nums.length - 1;
        while (l <= r) {
            int m = l + ((r - l) >>> 1);
            if (nums[m] == target) return true;

            if (nums[l] == nums[m] && nums[m] == nums[r]) {
                l++;
                r--;
                continue;
            }

            if (nums[l] <= nums[m]) {
                if (nums[l] <= target && target < nums[m]) r = m - 1;
                else l = m + 1;
            } else {
                if (nums[m] < target && target <= nums[r]) l = m + 1;
                else r = m - 1;
            }
        }
        return false;
    }
}
```

## 复杂度分析
- 时间：平均 \(O(\log n)\)，最坏 \(O(n)\)
- 空间：\(O(1)\)

## 相关题目
- 33 搜索旋转排序数组：无重复版
