# 871. 最低加油次数

- 题号：871
- 难度：困难
- LeetCode：[最低加油次数](https://leetcode.cn/problems/minimum-number-of-refueling-stops/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 贪心 + 大根堆：沿路走，遇到可达的加油站就把油量加入堆；当油不够到下一个位置时，从堆里取最大油量加一次。
- 取不到说明无法到达。

## Java 最佳实现

```java
import java.util.PriorityQueue;

class Solution {
    public int minRefuelStops(int target, int startFuel, int[][] stations) {
        PriorityQueue<Integer> pq = new PriorityQueue<>((a, b) -> Integer.compare(b, a));
        long fuel = startFuel;
        int ans = 0;
        int i = 0;

        while (fuel < target) {
            while (i < stations.length && stations[i][0] <= fuel) {
                pq.add(stations[i][1]);
                i++;
            }
            if (pq.isEmpty()) return -1;
            fuel += pq.poll();
            ans++;
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n\log n)\)
- 空间：\(O(n)\)

## 相关题目
- 1642 可以到达的最远建筑：同样是“堆 + 贪心”
