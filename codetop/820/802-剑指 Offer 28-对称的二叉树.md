# 剑指 Offer 28. 对称的二叉树

- 题号：剑指 Offer 28
- 难度：简单
- LeetCode：[对称的二叉树](https://leetcode.cn/problems/dui-cheng-de-er-cha-shu-lcof/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 递归判断两棵树是否镜像：`left.val == right.val` 且 `left.left` 对 `right.right`、`left.right` 对 `right.left` 都对称。

## Java 最佳实现

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode(int x) { val = x; }
 * }
 */
class Solution {
    public boolean isSymmetric(TreeNode root) {
        return root == null || same(root.left, root.right);
    }

    private boolean same(TreeNode a, TreeNode b) {
        if (a == null && b == null) return true;
        if (a == null || b == null) return false;
        if (a.val != b.val) return false;
        return same(a.left, b.right) && same(a.right, b.left);
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)

## 相关题目
- 101 对称二叉树：同题
