# 278. 第一个错误的版本

- 题号：278
- 难度：简单
- LeetCode：[第一个错误的版本](https://leetcode.cn/problems/first-bad-version/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 二分：在 `[1..n]` 找第一个 `isBadVersion(mid)==true` 的位置。

## Java 最佳实现

```java
/* The isBadVersion API is defined in the parent class VersionControl.
      boolean isBadVersion(int version); */

public class Solution extends VersionControl {
    public int firstBadVersion(int n) {
        int l = 1, r = n;
        int ans = n;
        while (l <= r) {
            int m = l + ((r - l) >>> 1);
            if (isBadVersion(m)) {
                ans = m;
                r = m - 1;
            } else {
                l = m + 1;
            }
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(\log n)\)
- 空间：\(O(1)\)

## 相关题目
- 34 在排序数组中查找元素的第一个和最后一个位置：二分边界
