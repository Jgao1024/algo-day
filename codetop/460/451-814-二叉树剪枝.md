# 814. 二叉树剪枝

- 题号：814
- 难度：中等
- LeetCode：[二叉树剪枝](https://leetcode.cn/problems/binary-tree-pruning/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 后序遍历：先递归剪掉左右子树，再判断当前节点：
  - 若左右都为空且 `val == 0`，则返回 `null`（剪掉）
  - 否则保留该节点

## Java 最佳实现

```java
/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public TreeNode pruneTree(TreeNode root) {
        if (root == null) return null;
        root.left = pruneTree(root.left);
        root.right = pruneTree(root.right);
        if (root.val == 0 && root.left == null && root.right == null) {
            return null;
        }
        return root;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(h)\)（递归栈）

## 相关题目
- 112 路径总和：树 DFS
- 543 二叉树的直径：后序汇总
