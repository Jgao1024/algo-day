# 369. 给单链表加一

- 题号：369
- 难度：中等
- LeetCode：[给单链表加一](https://leetcode.cn/problems/plus-one-linked-list/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 递归从尾到头处理进位：返回“当前节点加一后产生的进位”。回溯时更新 `node.val`，最后若头结点仍有进位则新建一个头结点。

## Java 最佳实现

```java
/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode plusOne(ListNode head) {
        int carry = dfs(head);
        if (carry == 0) return head;
        ListNode node = new ListNode(1);
        node.next = head;
        return node;
    }

    private int dfs(ListNode node) {
        if (node == null) return 1; // 尾部 +1
        int carry = dfs(node.next);
        int sum = node.val + carry;
        node.val = sum % 10;
        return sum / 10;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)（递归栈）

## 相关题目
- 2 两数相加：链表加法
- 445 两数相加 II：正序链表加法
