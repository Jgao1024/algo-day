# 983. 最低票价

- 题号：983
- 难度：中等
- LeetCode：[最低票价](https://leetcode.cn/problems/minimum-cost-for-tickets/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 动态规划按“天”推进（天数最多 365）：
  - 设 `travel[day]` 表示这天要不要出行
  - `dp[i]` 表示覆盖到第 `i` 天的最低花费
  - 若不出行：`dp[i] = dp[i-1]`
  - 若出行：`dp[i] = min(dp[i-1]+c1, dp[i-7]+c7, dp[i-30]+c30)`（下标小于 0 视为 0）

## Java 最佳实现

```java
class Solution {
    public int mincostTickets(int[] days, int[] costs) {
        int last = days[days.length - 1];
        boolean[] travel = new boolean[last + 1];
        for (int d : days) {
            travel[d] = true;
        }

        int[] dp = new int[last + 1];
        for (int i = 1; i <= last; i++) {
            if (!travel[i]) {
                dp[i] = dp[i - 1];
            } else {
                int c1 = dp[i - 1] + costs[0];
                int c7 = dp[Math.max(0, i - 7)] + costs[1];
                int c30 = dp[Math.max(0, i - 30)] + costs[2];
                dp[i] = Math.min(c1, Math.min(c7, c30));
            }
        }
        return dp[last];
    }
}
```

## 复杂度分析
- 时间：\(O(L)\)，\(L\le 365\)
- 空间：\(O(L)\)

## 相关题目
- 322 零钱兑换：经典 DP
- 1186 删除一次得到子数组最大和：状态 DP
