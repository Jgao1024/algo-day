# 面试题 04.01. 节点间通路

- 题号：面试题 04.01
- 难度：中等
- LeetCode：[节点间通路](https://leetcode.cn/problems/route-between-nodes-lcci/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **BFS/DFS 可达性**  
在有向图中从 `start` 出发做 BFS，能到达 `target` 就返回 `true`，否则 `false`。

## Java 最佳实现

```java
import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.List;
import java.util.Queue;

class Solution {
    public boolean findWhetherExistsPath(int n, int[][] graph, int start, int target) {
        List<Integer>[] g = new List[n];
        for (int i = 0; i < n; i++) g[i] = new ArrayList<>();
        for (int[] e : graph) g[e[0]].add(e[1]);

        boolean[] vis = new boolean[n];
        Queue<Integer> q = new ArrayDeque<>();
        q.add(start);
        vis[start] = true;
        while (!q.isEmpty()) {
            int u = q.poll();
            if (u == target) return true;
            for (int v : g[u]) {
                if (!vis[v]) {
                    vis[v] = true;
                    q.add(v);
                }
            }
        }
        return false;
    }
}
```

## 复杂度分析
- 时间：\(O(V+E)\)
- 空间：\(O(V+E)\)

## 相关题目
- 1971 寻找图中是否存在路径：无向图版
