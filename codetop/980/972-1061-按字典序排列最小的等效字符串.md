# 1061. 按字典序排列最小的等效字符串

- 题号：1061
- 难度：中等
- LeetCode：[按字典序排列最小的等效字符串](https://leetcode.cn/problems/lexicographically-smallest-equivalent-string/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **并查集 + 选字典序最小代表元**  
把 26 个小写字母当成节点。把 `s1[i]` 和 `s2[i]` 联通；合并时让集合代表元为字典序更小的字符。  
最后对 `baseStr` 的每个字符，替换成它所在集合的代表元即可得到最小等效字符串。

## Java 最佳实现

```java
class Solution {
    public String smallestEquivalentString(String s1, String s2, String baseStr) {
        UnionFind uf = new UnionFind(26);
        for (int i = 0; i < s1.length(); i++) {
            uf.union(s1.charAt(i) - 'a', s2.charAt(i) - 'a');
        }
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < baseStr.length(); i++) {
            int r = uf.find(baseStr.charAt(i) - 'a');
            sb.append((char) ('a' + r));
        }
        return sb.toString();
    }

    static class UnionFind {
        int[] p;

        UnionFind(int n) {
            p = new int[n];
            for (int i = 0; i < n; i++) p[i] = i;
        }

        int find(int x) {
            if (p[x] != x) p[x] = find(p[x]);
            return p[x];
        }

        void union(int a, int b) {
            int ra = find(a);
            int rb = find(b);
            if (ra == rb) return;
            if (ra < rb) p[rb] = ra;
            else p[ra] = rb;
        }
    }
}
```

## 复杂度分析
- 时间：\(O((|s1|+|baseStr|)\cdot \alpha(26))\)
- 空间：\(O(1)\)

## 相关题目
- 547 省份数量：并查集
