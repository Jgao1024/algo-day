# 383. 赎金信

- 题号：383
- 难度：简单
- LeetCode：[赎金信](https://leetcode.cn/problems/ransom-note/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **计数**  
用 26 长度数组统计 `magazine` 每个字母数量，再遍历 `ransomNote` 依次消耗；若某个字母不够，返回 `false`。

## Java 最佳实现

```java
class Solution {
    public boolean canConstruct(String ransomNote, String magazine) {
        int[] cnt = new int[26];
        for (int i = 0; i < magazine.length(); i++) {
            cnt[magazine.charAt(i) - 'a']++;
        }
        for (int i = 0; i < ransomNote.length(); i++) {
            int idx = ransomNote.charAt(i) - 'a';
            if (cnt[idx] == 0) return false;
            cnt[idx]--;
        }
        return true;
    }
}
```

## 复杂度分析
- 时间：\(O(n+m)\)
- 空间：\(O(1)\)

## 相关题目
- 242 有效的字母异位词：计数比较
