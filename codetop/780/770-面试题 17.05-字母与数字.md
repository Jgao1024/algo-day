# 面试题 17.05. 字母与数字

- 题号：面试题 17.05
- 难度：中等
- LeetCode：[字母与数字](https://leetcode.cn/problems/find-longest-subarray-lcci/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- **前缀差 + 哈希表记录最早位置**  
把“字母”记为 +1，“数字”记为 -1。前缀和 `diff` 相同的两个位置之间，字母和数字数量相等。  
用 `map` 记录每个 `diff` 最早出现的下标，扫描过程中更新最长区间。

## Java 最佳实现

```java
import java.util.HashMap;
import java.util.Map;

class Solution {
    public String[] findLongestSubarray(String[] array) {
        Map<Integer, Integer> first = new HashMap<>();
        first.put(0, -1);

        int diff = 0;
        int bestLen = 0;
        int bestL = 0;
        int bestR = -1;

        for (int i = 0; i < array.length; i++) {
            String s = array[i];
            char c = s.charAt(0);
            if (c >= '0' && c <= '9') diff--;
            else diff++;

            Integer j = first.get(diff);
            if (j == null) {
                first.put(diff, i);
            } else {
                int len = i - j;
                if (len > bestLen) {
                    bestLen = len;
                    bestL = j + 1;
                    bestR = i;
                }
            }
        }

        if (bestLen == 0) return new String[0];
        String[] ans = new String[bestLen];
        for (int i = 0; i < bestLen; i++) {
            ans[i] = array[bestL + i];
        }
        return ans;
    }
}
```

## 复杂度分析
- 时间：\(O(n)\)
- 空间：\(O(n)\)

## 相关题目
- 525 连续数组：0/1 数量相等的最长子数组
