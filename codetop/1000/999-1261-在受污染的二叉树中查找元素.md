# 1261. 在受污染的二叉树中查找元素

- 题号：1261
- 难度：中等
- LeetCode：[在受污染的二叉树中查找元素](https://leetcode.cn/problems/find-elements-in-a-contaminated-binary-tree/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- DFS 还原 + HashSet（推荐）  
污染树满足：根为 0；若节点值为 `x`，则左子为 `2x+1`，右子为 `2x+2`。  
构造时 DFS 还原所有节点值并加入集合，`find(target)` 就是判断集合是否包含。

## Java 最佳实现

```java
import java.util.HashSet;
import java.util.Set;

class FindElements {
    private final Set<Integer> set = new HashSet<>();

    public FindElements(TreeNode root) {
        recover(root, 0);
    }

    public boolean find(int target) {
        return set.contains(target);
    }

    private void recover(TreeNode node, int val) {
        if (node == null) return;
        node.val = val;
        set.add(val);
        recover(node.left, val * 2 + 1);
        recover(node.right, val * 2 + 2);
    }
}
```

## 复杂度分析
- 时间：构造 \(O(n)\)，查询 \(O(1)\)
- 空间：\(O(n)\)

## 相关题目
- 173 二叉搜索树迭代器（构造 + 查询类）

## 总结（速记）
- **构造时把值都恢复出来放到集合里**。
