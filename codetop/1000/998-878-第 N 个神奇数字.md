# 878. 第 N 个神奇数字

- 题号：878
- 难度：困难
- LeetCode：[第 N 个神奇数字](https://leetcode.cn/problems/nth-magical-number/)

## 题目描述
见题面链接。

## 示例
见题面链接。

## 最佳解法思路
- 二分答案（推荐）  
神奇数字是能被 `a` 或 `b` 整除的数。  
对某个 `x`，满足条件的个数：`x/a + x/b - x/lcm(a,b)`。  
二分找最小的 `x` 使计数 \(\ge n\)，最后对 \(1e9+7\) 取模。

## Java 最佳实现

```java
class Solution {
    public int nthMagicalNumber(int n, int a, int b) {
        long MOD = 1_000_000_007L;
        long lcm = lcm(a, b);
        long left = 1;
        long right = (long) n * Math.min(a, b);
        while (left < right) {
            long mid = left + (right - left) / 2;
            long cnt = mid / a + mid / b - mid / lcm;
            if (cnt >= n) right = mid;
            else left = mid + 1;
        }
        return (int) (left % MOD);
    }

    private long gcd(long x, long y) {
        while (y != 0) {
            long t = x % y;
            x = y;
            y = t;
        }
        return x;
    }

    private long lcm(long x, long y) {
        return x / gcd(x, y) * y;
    }
}
```

## 复杂度分析
- 时间：\(O(\log(n\cdot \min(a,b)))\)
- 空间：\(O(1)\)

## 相关题目
- 668 乘法表中第 k 小的数（同样二分答案）

## 总结（速记）
- **二分 x，计数用容斥：/a + /b - /lcm**。
